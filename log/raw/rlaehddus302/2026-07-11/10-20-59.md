## [turn] 2026-07-11 10:20:59

### user
업데이터 하고 코드도 업데이트 해

### assistant






Hermes 자신의 Mattermost 어댑터가 실제로 읽는 환경변수 이름이 `MATTERMOST_TOKEN`(우리 코드의 `MATTERMOST_BOT_TOKEN`과 이름이 다름)이라는 걸 확인했으니, 이름부터 맞춰서 그대로 재사용 가능하게 코드도 같이 고치겠습니다.







Good, consistent. Now let's update the README.





코드와 README 둘 다 업데이트했습니다.

**코드 변경** (`mattermost_search_mcp.py`):
- 읽는 환경변수 이름을 `MATTERMOST_BOT_TOKEN` → **`MATTERMOST_TOKEN`**으로 변경. Hermes 자신의 Mattermost 어댑터(`adapter.py`, `gateway/config.py`)가 읽는 이름과 정확히 맞춰서, 같은 `.env` 값을 그대로 참조할 수 있게 함
- 모듈 docstring에 이 이름 일치가 의도적이라는 것과, `team_id`는 이 서버 전용값이라 겹치지 않는다는 것도 명시

**README 변경**:
- "Hermes에 등록" 섹션에 `${VAR}` 치환 방식 추가 — `~/.hermes/.env`에 토큰/URL을 한 번만 적고, `config.yaml`에서는 `${MATTERMOST_TOKEN}`처럼 참조만 하는 예시로 교체
- 왜 이게 동작하는지(`load_hermes_dotenv()` → `${VAR}` 치환 → `_build_safe_env`가 명시된 값만 서브프로세스에 전달)를 코드 근거와 함께 설명

이제 Hermes 어댑터 설정과 우리 MCP 서버 설정이 같은 `.env` 값을 공유해서, 토큰이 바뀌어도 한 곳만 고치면 됩니다.
