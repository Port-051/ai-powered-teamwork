# 2026-07-03 shj5771

## 기획서 docx 작업 (목차·표 서식)
- GitHub에 올라간 `submission/기획서_TeamBrain_Port051.docx`에 목차가 없다는 걸 발견 → 원본 docx 구조를 참고해 목차를 XML 수준에서 직접 추가 (python-docx 미설치라 zipfile/XML 직접 조작).
- 커밋·푸시 과정에서 브랜치가 원격과 갈라져 있어(다른 팀원이 먼저 push함) pull/rebase 후 push, 이후 재차 충돌이 생겨 다시 받아오고 push하는 과정을 반복함.
- "개발환경" 섹션 표에서 항상 비어있던 마지막 열을 정리 요청 받음 → 처음엔 열 너비만 줄였다가, 사용자가 "세부 내용" 열 자체를 통째로 지워야 한다고 재지시 → 열 완전 삭제 + 헤더 행도 함께 정리.
- 목차 위치를 표지 다음 페이지(2페이지)로 옮겨달라는 요청 처리, 이어서 "1. 프로젝트 요약" 섹션(표 포함)을 제목 바로 아래(목차 앞)로 이동.
- 이후 "1. 프로젝트 요약" 섹션 자체가 텍스트 분량이 많아(2만자 이상) 목차가 자연스럽게 3페이지로 밀리는 문제 확인 — 페이지 나누기 버그가 아니라 콘텐츠 분량 때문이라고 설명. 간격을 줄여 압축하는 방향으로 진행하려던 중 대화가 커밋/푸시로 넘어감.
- 목차 페이지 번호가 전부 "1"로 나오는 문제 발견 — TOC 필드가 Word에서 열릴 때 실제 계산돼야 하는데 아직 한 번도 계산 안 된 상태라 그런 것이라고 설명, 파일 구조 자체는 정상 확인.
- 마지막에 팀원이 Windows에서 수기로 직접 수정한 파일(`기획서_TeamBrain_Port051.docx`)을 받아서 저장소에 반영, 커밋 진행.

## 결정된 것
- "개발환경" 표에서 "세부 내용" 열 완전 삭제, 표는 "구분 | 항목 | 세부 내용" → 실제로는 열 삭제 후 재구조.
- 목차는 표지 다음 페이지(2페이지)에 위치, "1. 프로젝트 요약"을 제목 바로 아래로 이동.

## 남은 질문 / 미해결
- 목차 페이지 번호가 전부 "1"로 표시되는 문제 (Word에서 파일을 열어 필드 업데이트해야 해결됨 — 사용자 액션 필요할 수 있음).
- 목차가 3페이지로 밀리는 문제는 "1. 프로젝트 요약" 분량 압축 작업 중간에 대화가 종료되어 완결 안 됨.
