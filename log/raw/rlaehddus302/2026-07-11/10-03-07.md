## [turn] 2026-07-11 10:03:07

### user
그래 어차피 쓰기 관리는 이미 있으니 별 필요 없고 읽기를 통해서 정보를 가져오는 게 중요하니 그렇게 해

### assistant








Now let's update the README to reflect the 6 tools.





조회 계열 6개 도구로 확장 완료했습니다.

**추가된 도구:**
- `search_mattermost` — 키워드/사람/채널 검색 (기존, from_user/in_channel 파라미터 포함)
- `get_channel_recent_posts` — 검색어 없이 채널 최근 대화 시간순 조회
- `get_thread` — 스레드 전체(원글+답글) 조회 — 지난번 "스레드 검색은 안 되나" 질문에서 나온 빈자리를 채움
- `get_user_by_username` / `get_user_by_id` — username ↔ user_id 변환 (from_user 쓸 때 필요)
- `list_channels_for_team` — 봇이 접근 가능한 채널 목록 확인 ("어디서 찾아야 할지" AI가 스스로 판단하는 시작점)

각 도구 docstring에 "이 상황엔 이 도구 대신 저 도구가 낫다"는 교차 안내를 넣어서, LLM이 사용자 질문의 성격(키워드 검색 vs 시간순 훑기 vs 스레드 맥락)에 따라 스스로 고를 수 있게 했습니다. 쓰기/관리 API는 말씀하신 대로 그대로 뺐습니다.

README도 6개 도구 표, config.yaml의 `include` 목록, 테스트 질문 예시로 업데이트했습니다.
