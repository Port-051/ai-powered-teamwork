## [turn] 2026-07-11 09:20:53

### user
이거 확실한 지 hermes 코드 뒤져서 확인 해 봐

### assistant


Hermes 로컬 소스(`/home/rlaehddus302/.hermes/hermes-agent`)를 직접 뒤져서 확인하겠습니다.

























확인 완료. 제가 이전에 말씀드린 게 맞다는 걸 코드로 직접 증명했습니다.

**확인한 3가지 증거:**

1. **`plugins/platforms/mattermost/adapter.py`** — 이 어댑터가 실제로 호출하는 Mattermost API 엔드포인트는 딱 6개뿐입니다: `posts`(메시지 전송), `users/me`, `posts/{id}`, `channels/{id}`, `files/{id}/info` — 전부 "봇이 멘션된 그 순간 응답하는 데 필요한" 최소한의 호출입니다. `search`나 `system_console` 같은 검색/관리자 API는 코드 전체에 단 한 줄도 없습니다.

2. **`toolsets.py`의 `hermes-mattermost` 툴셋 정의** (line 498):
   ```python
   "hermes-mattermost": {
       "tools": _HERMES_CORE_TOOLS,   # 플랫폼 전용 도구 없음
       "includes": []
   }
   ```
   반면 바로 위 `hermes-discord` 툴셋은 `_HERMES_CORE_TOOLS + ["discord", "discord_admin"]`로, **Discord 전용 조회 도구**(`tools/discord_tool.py` — "Discord server introspection and management tool", 서버 검색·멤버 조회 등)가 따로 얹혀 있습니다. **Mattermost에는 이 짝이 되는 `mattermost_tool.py` 자체가 존재하지 않습니다.** Discord는 봇이 대화 중에 "서버 검색해봐"를 스스로 호출할 수 있는데, Mattermost는 그 기능이 애초에 구현이 안 돼 있는 겁니다.

3. **`_HERMES_CORE_TOOLS`에 있는 유일한 "검색" 도구 `session_search`**는 `tools/session_search_tool.py`를 보면 명시적으로 "Hermes 자신의 SQLite 세션 DB(FTS5 인덱스)"만 검색합니다. Mattermost의 DB나 API가 아니라 **Hermes가 그동안 나눈 대화 기록**을 검색하는 도구입니다.

**결론**: 시스템 관리자 권한을 아무리 줘도 Hermes 봇이 스스로 Mattermost를 검색할 코드 경로가 아예 없습니다. Discord처럼 플랫폼 전용 introspection 도구를 만들어야 하는데, 그게 지금 없다는 뜻은 — 기획서의 "팀 협업 지능 레이어"가 바로 이 빈자리(Discord엔 있고 Mattermost엔 없는 `mattermost_tool.py` 같은 것)를 채우는 작업이라는 걸 코드 수준에서 재확인한 셈입니다.
