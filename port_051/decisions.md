# 팀 의사결정 기록

## 2026-07-03
- git 워크트리는 쓰지 않는다. stash로 충분하고, 팀원 대부분 git 경험이 적어 폴더가 여러 개로 나뉘면 혼란만 커진다는 판단.
- `submission/기획서_TeamBrain_Port051.docx` 작업 시 `submission/reference/기획서_원본양식.docx`(SW Maestro 17기 제출 양식 원본)를 절대 수정하지 않는 비교 기준 파일로 둔다. 문서에서 파란 글씨는 "수정 필요(채워 넣을 자리)", 검은 글씨는 "수정 금지(그대로 유지)"를 의미하므로 이 규칙을 지켜서 채운다.
- 목차(TOC)는 새로 만들지 않고 원본 양식 파일의 목차 표 XML을 그대로 복사해서 쓴다 — 직접 재구성하면 서식이 계속 원본과 어긋남.
- 모든 표에 행 분할 방지(cantSplit) 옵션을 적용해 표가 페이지 경계에서 잘리지 않게 한다.

## 2026-07-04
- `submission/기획서_TeamBrain_Port051.docx`/`.pdf`는 여운호(unhoyeo)가 수작업으로 최종 정리한 버전을 기준으로 한다 — 다른 팀원의 로컬 수정과 충돌하면 이 최종본을 우선한다.
- git 커밋용 김동연(rlaehddus302)의 메인 이메일은 `kimyeon30@naver.com` (Claude Code 연동 계정 이메일과는 다름).
- 팀 캘린더(구글 캘린더 프로토타입)는 개인 구글 계정 기반으로 운영하고, 별도 팀 전용 계정은 만들지 않는다.
- 멘토링/특강 일정 이름 형식: `멘토링 (멘토님 성함, 장소)`, `특강 (...)`.
- 일정이 등록/변경/삭제될 때마다 팀원 전체(김동연 `2020112071@dgu.ac.kr`, 신홍재 `shj2224@naver.com`)에게 실시간 이메일 알림을 보낸다 (Google Apps Script 트리거 기반, 폴링 방식 아님).

## 2026-07-05
- 팀 캘린더 변경 알림 이메일 포맷 최종 확정: 발신자 표시 이름 `Port_051`, 제목 `팀 캘린더 변경 알림` 고정, 본문은 `[신규 등록]/[변경 전]/[변경 후]/[삭제됨]` 구분 + 글머리 기호로 표시.
- 이 알림은 Google Calendar의 `onEventUpdated` 트리거 기반이라, Claude를 거치지 않고 캘린더 앱에서 직접 수동으로 일정을 추가/수정/삭제해도 동일하게 이메일이 간다 — 팀원들은 굳이 Claude를 통하지 않고 캘린더 앱에서 바로 편집해도 됨.

## 2026-07-06
- 팀 캘린더 변경 알림 이메일 제목을 유형별로 분리: `팀 캘린더 등록 알림` / `팀 캘린더 변경 알림` / `팀 캘린더 삭제 알림` (기존엔 세 경우 모두 `팀 캘린더 변경 알림`으로 동일했음). Apps Script에서 등록/변경/삭제 건이 있을 때마다 각각 별도 메일로 발송하도록 수정.
- **자연어로 "캘린더에 일정 등록해줘" 하는 지금 방식은 여운호(unhoyeo) 로컬 컴퓨터 전용이다.** 구글 계정 인증 토큰(`~/.claude/gcal/`)이 여운호 개인 계정으로 한 번 로그인해서 발급받은 것이라, 다른 팀원이 자기 컴퓨터에서 Claude Code를 켜도 이 토큰이 없어서 못 씀. 팀원 전체가 쓰게 하려면 두 방향 중 택1:
  1. (간단) 팀원마다 자기 컴퓨터에서 구글 로그인 한 번씩 거쳐 각자 토큰 발급받기.
  2. (정식, PLAN.md 방향) 이 로직을 Mattermost 봇 뒤의 별도 서버 프로세스로 옮기기 — 필요한 것: ①Mattermost 서버(순정, 자체 호스팅) ②Mattermost 어댑터(멘션 인식) ③자연어→캘린더 액션 변환 로직(LLM 호출) ④팀 공용 구글 인증(서버용 토큰 또는 Service Account) ⑤상시 켜져 있는 서버. 지금 이 구글 캘린더 스크립트는 사실 ②"팀 공유 인텔리전스 레이어" 아이디어의 수동 프로토타입.
  - 아직 어느 방향으로 갈지는 미정 (2026-07-06 기준 열린 질문).
- **PLAN.md에 "팀 협업 지능 레이어" 상세 설계 확정 (Phase 4-1 신설, unhoyeo가 반영/커밋).** ⚠️ **2026-07-08 정정**: 이 항목은 실제로는 main의 PLAN.md에 병합된 적이 없다 — 당시 작업이 `worktree-squishy-questing-pine`이라는 별도 워크트리 브랜치에서만 커밋되고 main엔 안 올라간 채 방치돼 있었음 (2026-07-08에 뒤늦게 발견, 브랜치는 삭제하고 내용은 `port_051/knowledge-layer-design-drafts.md`의 "초안 B"로 보존). 아래 내용은 "확정된 사실"이 아니라 "그 초안의 서술"로 읽을 것 — 이게 프로젝트의 유일한 오리지널 IP라 구체화했었다는 기록만 남긴다:
  - 데이터 흐름 4단계: 캡처(Mattermost 이벤트) → 추출·정제(지식 단위로 가공) → 저장·색인(임베딩 + 메타데이터) → 검색·주입(다음 `@ai` 호출 시 컨텍스트로 반영).
  - **지식 단위(Knowledge Unit) 종류**: 결정(Decision) / 사실·노하우(Fact) / 미해결 이슈(OpenQuestion) / 용어(Glossary). 각 단위는 출처 스레드·채널·작성자·타임스탬프·신뢰도를 메타데이터로 가짐.
  - **지식 추출은 MVP 단계에선 명시적 등록(`@ai 이거 기억해`)부터 시작**하고, 검증되면 "스레드가 닫힐 때 자동 요약·추출"을 나중에 얹는다.
  - **개인 DM 대화는 팀 지식으로 승격하지 않는다 — 공개 채널 대화만 팀 지식 후보** (권한/비밀정보 관리 원칙과 일치).
  - Phase 5(차별점), Phase 7(검증 계획), Phase 8(열린 질문)도 이 설계에 맞춰 함께 갱신됨. 세부 내용은 항상 PLAN.md가 최신 기준.
- **Hermes 메모리를 팀용으로 변환하는 방향 확정 (멘토 김선만 제안).** Hermes의 1인용 3층 메모리(영속 사실 / SessionDB / 스킬)와 그 하부 저장소 Honcho(workspace·peer·session)에 "소유자·가시성 태그"를 얹어 개인→팀으로 집계·공유하는 레이어를 만든다 = 우리가 새로 짜는 핵심. 매핑: Mattermost 팀→Honcho workspace, 팀원→peer, 채널/스레드→session. **★최우선 확인 과제: Honcho가 "한 session에 여러 peer + peer별 기억 격리"를 지원하는지** (이 답이 팀 전환 난이도 전체를 좌우).
- **역할 분담**: 김동연=인프라(Mattermost 설치·봇·API·Docker), 여운호=AI 엔진(Hermes 로컬 실행·메모리 구조·Honcho 모델), 신홍재=팀 레이어(어댑터·권한 설계·대시보드). 학습·구축 로드맵은 `port_051/roadmap.md` 참고.
- `submission/기획서_draft.md`의 "문제인식" 섹션을 팀 실경험 기반으로 재작성(커밋 완료). 병목이 정보→합의로 이동했다는 관점 + 우리 팀이 주제 정하는 데 몇 주 걸린 경험 + 시나리오 3개(같은 질문 반복 / 결정이 사람 기억에만 남음 / bus factor) + 기존 해법 한계.

## 2026-07-07
- **7/6 "Hermes→Honcho 매핑(workspace/peer/session)" 결정 보정**: Honcho는 Hermes가 지원하는 8종 외부 메모리 provider 중 하나일 뿐이고 한 번에 하나만 활성화 가능함이 확인됨(김동연) — 채택이 필수가 아니라 선택 사항. 이는 PLAN.md Phase 8의 기존 결정("1차는 Mattermost DB만 사용, 나머지는 Hermes가 담당 — 별도 검색/요약 레이어 미도입")과 이미 일치하므로, "Honcho 멀티-peer 확인이 최우선 과제"였던 7/6 항목은 사실상 해소됨. Honcho의 peer 기반 멀티유저 구조는 설계 참고 자료로만 남긴다.
- **MVP "팀 공유 지능" 구현 방식 재확인**(김동연): 지식 단위 분류·임베딩 검색 없이, 어댑터가 Mattermost 검색 API로 관련 메시지를 가져와 개인 Hermes 기억과 합쳐 프롬프트에 넣는 단순 버전으로 7월 MVP 핵심 체험(팀 대화를 참고해 답하기)이 가능함을 재확인. **권한 처리도 사실상 공짜로 해결** — 사용자 본인 토큰으로 검색하면 비공개 채널은 자동으로 검색 결과에서 빠짐. (정교한 지식 단위 분류·임베딩·세밀한 가시성 태그는 8~9월 고도화 대상으로 그대로 유지.)
- **팀 협업 인텔리전스 레이어 5단계 파이프라인 초안**(신홍재, PLAN.md Phase 8 8월 재설계용 사전 스케치 — 아직 PLAN.md 미반영): 수집(Ingest, 멘션 무관 채널 전체·스레드 단위) → 선별(Filter/Classify, 저비용 모델로 결정/노하우/사실 vs 잡담) → 추출(Extract, `{유형,요약,참여자,채널,원본링크,시각}` 구조화) → 저장(Store, 임베딩+벡터DB, 개인/팀공유 권한 태그·기본값 팀공유) → 검색/응답(Retrieve, 개인 기억+팀 공유 저장소를 Hermes에 tool로 노출). "원문 저장 후 RAG 검색"보다 "실시간 요약·구조화 축적"을 추천. 미결정: (a) 실시간 vs 배치 처리 (b) 권한 태깅 자동 vs 수동.
- **워크트리 자동격리 끔** — `.claude/settings.json`에 `"worktree": {"bgIsolation": "none"}` 추가(여운호, 커밋 완료). 이유: `log/raw/` 자동 push 훅이 항상 main 체크아웃을 전제하는데 백그라운드 세션의 기본 워크트리 격리와 충돌해 로그 자동화가 깨졌었음 — 이 프로젝트는 팀원 1명이 이 폴더에서 Claude Code를 켜고 항상 main에 바로 커밋하는 단일 브랜치 워크플로우라 워크트리 격리 자체가 불필요.
- **팀원 실습 폴더명 통일**: 한글 실명 → 영문(`unho`/`dongyeon`/`hongjae`)으로 변경(커밋 완료). 앞으로 `experiments/` 하위 팀원 폴더는 이 영문명 기준.
- **랜딩페이지 구성 확정**: 최소 원페이지 Next.js — ①한 줄 정의 ②문제 제시 ③솔루션 3단계 ④이메일 폼 수집.
- PLAN.md Phase 2-1(1차 베타 타겟: 자체 메신저 없는 보안 민감 중소기업)·Phase 2-2(GTM 순서 후보)·Phase 4-1(핵심 기술 챌린지: 프라이버시 필터+지식 체계화 엔진)·Phase 5(슬랙 대비 차별점) 신설·보강(여운호, 최학열 멘토링 반영). 상세는 PLAN.md와 `meetings/2026-07-07_최학열-멘토링.md` 참고 — 이 문서들이 항상 최신 기준.

## 2026-07-09
- **Mattermost↔Hermes 통합 지점 근거 재정리(여운호·김동연)**: 기획서의 "Mattermost → 우리 어댑터 → Hermes"라는 문장이 근거 없이 그려진 그림이었음을 발견해 소스 코드로 재검증함. 어댑터 새로 짜기 / 코어 포크 / api_server 단독 / 이벤트 훅 / memory-provider 5가지를 같은 기준으로 비교한 결과, **memory-provider 확장점(`agent/memory_provider.py`)이 라이선스상 안전하고(Hermes=MIT 프로세스 내부 확장점이라 Mattermost AGPL과 무관) 실익도 크다는 근거가 마련됨** — 아직 "8월 재결정 확정"은 아니고 근거 정리 단계. 상세는 `port_051/mattermost-hermes-integration-decision.md`(쉬운 설명판: `port_051/hermes-개인화-쉬운정리.md`) 참고.
- **개인화(기획서 주요기능3)는 세션 격리(레벨1)만으로는 성립 불가함을 코드로 확인(여운호)**: `USER.md`(AI가 학습한 사용자 정보)가 세션이 아니라 프로파일 단위로만 존재함이 확인됨. 진짜 "나를 학습하는 개인 AI"를 만들려면 프로파일 분리(레벨2)가 필요하고, 검토했던 방법 C(api_server 주소로 사람 구분)는 코드 구조상 실현 불가로 확정 제외됨 — 남은 후보는 방법 A(봇 계정별 프로파일)/B(발신자 감지 코드)/D(Hermes를 인원수만큼 복제), 채택은 아직 미정.
- **랜딩페이지는 Vite 기반 스캐폴딩을 기준본으로 채택(신홍재)**: 신홍재가 로컬에서 Next.js로 재구성하던 중, 다른 팀원이 이미 원격에 올려둔 Vite 기반 개편판("랜딩 페이지 구조 개편 및 개인 AI 설명 강화" 커밋)과 충돌을 rebase 중 발견 — 로컬 Next.js본은 폐기하고 Vite 버전을 채택함.

## 2026-07-10
- **중간평가까지 "MVP 빨리 만들고 배포부터" 우선순위 확정(김선만 멘토링)**: 세부 완성도보다 실제로 돌아가는 배포본을 먼저 만드는 쪽으로 우선순위를 정함.
- **랜딩페이지 1차 결과물 배포 완료**: React + AWS(CloudFront)로 배포, URL `https://d35i71sym3m7f7.cloudfront.net/`. (2026-07-09 결정과 이어지는 Vite 기반 스캐폴딩의 배포본으로 추정 — Next.js 아님.)
- **다음 주(~7/16 MVP 마감, `roadmap.md` Phase 3-1과 동일 시점) 목표 재확인**: 헤르메스 + Mattermost를 Oracle Cloud에 호스팅 + 팀원별 봇 생성(개별 프로필) + 랜딩페이지 개선 + 기획안 완성 + Codex 연동을 OAuth·API 방식 둘 다 붙이기.
- **랜딩페이지에 "운영체제 제한 없음" 문구 추가 확정**: 모든 핸드폰/윈도우즈/아이폰/리눅스에서 접속 가능하다는 점을 어필 (Mattermost 기반이라 클라이언트 제약이 없다는 논리).
- **관리자페이지 기능 범위(미래, MVP 이후) 정리**: ①봇별 메시지 접근 권한 제어 ②관리자 계정·팀원 계정과 봇 생성 ③API 연동 + 디파짓(사용량 기반 과금, 먼 훗날) ④오픈소스화(1차 홍보 대상: 네이버·카카오·삼성전자·LG·SK·현대 등, 기술 고도화 병행). — MVP 범위 밖, `roadmap.md` Phase 4 이후 백로그로 취급.

## 2026-07-11
- **TeamBrain memory-provider 프로덕션 버전 착수(신홍재)**: 07-10 여운호 프로토타입(프라이버시 필터 없음)을 `teambrain/memory_provider/`로 정식화 — `chat_type == "channel"`(완전 공개 채널)일 때만 팀 공유 저장소에 기록, DM·비공개 채널·누락값은 fail-closed로 저장 안 함(roadmap.md Phase 3-4 완료 기준 충족). Mattermost 검색 API 위임 방식(초안 D)도 검토했으나 팀원별 개인 토큰 발급이 추가로 필요해 이번엔 채택 안 함. 상세 근거·한계·범위밖 항목은 `port_051/teambrain-memory-provider-mvp-visibility-decision.md` 참고.
