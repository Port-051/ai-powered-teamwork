## [turn] 2026-07-10 11:02:10

### user
Error: required flag(s) "email", "password", "username" not set

What's next:
    Try Docker Debug for seamless, persistent debugging tools in any container or image → docker debug my-mattermost
    Learn more at https://docs.docker.com/go/debug-cli/

### assistant


이건 여러 줄로 나눠 쓴 명령어의 줄바꿈(`\`)이 지금 사용 중인 셸에서 제대로 처리가 안 됐을 가능성이 높습니다 (예: PowerShell이나 cmd에서는 `\`가 줄바꿈 기호로 안 먹힙니다). 한 줄로 합쳐서 다시 시도해주세요.

```bash
docker exec -it my-mattermost mmctl user create --email test2@local.test --username test2 --password "Test12345!" --local
```

지금 이 명령어를 어떤 터미널(WSL 터미널인지, Windows PowerShell/cmd인지)에서 실행하고 계신가요? WSL이 아니라면 한 줄로 입력하는 게 제일 안전합니다.
