## [turn] 2026-07-08 02:11:35

### user
그럼, user_id 등 db에저장되는 칼럼이름은 기존에 mattermost가 사용하는데로 저장되겠네? 회사 db를 연결한다치면

### assistant


네, 맞습니다. Mattermost는 접속만 되면 **자기가 필요한 테이블·컬럼 구조를 전부 알아서 만듭니다** (마이그레이션이라는 절차로 자동 생성). 회사 MySQL 서버든 우리가 새로 띄운 DB든, Mattermost가 처음 연결되면 똑같은 구조(`users` 테이블에 `id`, `username`, `email` 등, `posts` 테이블에 `message`, `createat` 등)를 그대로 만듭니다 — 어디 서버냐와 무관하게 **컬럼 이름·구조는 Mattermost가 정한 대로 고정**됩니다.

한 가지 조건이 있어요: **그 DB(스키마)는 반드시 비어있는, Mattermost 전용으로 내줘야 합니다.** 회사가 이미 쓰던 다른 서비스 테이블이 있는 DB에 얹으면 안 되고, `mattermost`라는 이름의 새 데이터베이스(스키마)를 하나 만들어서 그 안에만 Mattermost가 자기 테이블들을 채우게 해야 합니다. 즉 "서버는 공유하되, 데이터베이스(스키마)는 전용으로" 쓰는 구조예요.
