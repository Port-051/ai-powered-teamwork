# 여운호 (unhoyeo) — 2026-07-08 작업 요약

이날은 대부분 **문서 정합성 정리(doc sync)**와 **레포/훅 위생 작업**이었고, 그 산출물은 이미 그날 바로 `port_051/`·PLAN.md·CLAUDE.md에 반영·커밋됨(아래는 무엇을 왜 했는지의 기록).

## 1. 전날치(07-07) teambrain-process 실행
- 서브에이전트 3개로 김동연(93건)·신홍재(95건)·여운호(48건) 07-07 raw를 병렬 처리 → `log/processed/<user>/2026-07-07.md` 생성, "팀 공유" 항목을 `port_051/`에 종합 반영, raw를 archive로 이동.

## 2. 기획 문서 전수 검토 → 불일치 6건 발견·정리
- PLAN.md와 `기획서_draft.md`가 따로 놀던 문제, "Mattermost 어댑터 이미 내장" 발견이 문서에 미반영, 고아 브랜치(main에 없는 PLAN 수정), 워크트리 5개 방치, 역할분담 문서별 상이, 활동비 계획서 레포 밖 등.
- 조치: PLAN.md Phase 1 문제인식을 정교한 버전으로 교체, "어댑터 이미 존재" 발견을 Phase 4/기획서 아키텍처·역할에 반영, Phase 2-3(세그먼트 계층: 0차 소마 드라이런~3차 오픈소스) 신설, 1차 타겟을 "자체 메신저 없음"(후보 A) vs "AI 관심 스타트업"(후보 B) 병렬 후보로 명시, Phase 7·8·9 갱신. `knowledge-layer-design-drafts.md`에 초안 C/D 추가, `decisions.md`의 잘못된 문구(Phase 4-1이 main에 병합됐다는 서술) 정정.

## 3. 레포/훅 위생 — 사고 수습 + 재발 방지
- **워크트리 5개 + 브랜치 3개 정리**: 각 워크트리의 커밋 안 된 변경이 이미 main에 반영됐는지 확인하고, 설계 초안은 `knowledge-layer-design-drafts.md`로 보존한 뒤 삭제(안전 확인 후).
- **`stop-log-push.sh` 범위 초과 버그 발견·수정**: `git commit`에 파일 범위 지정이 없어 그 순간 스테이징된 무관한 파일(settings.json, 훅)까지 함께 커밋·push되고 있었음("자동 push는 log/raw만"이라는 CLAUDE.md 원칙 위반, 실제로 훅 설정이 확인 없이 push되는 사고 발생). 커밋 명령에 `-- "$out"`을 붙여 로그 파일만 커밋되게 수정(임시 테스트 저장소로 검증).
- **브랜치/워크트리 생성 자체를 차단하는 PreToolUse 훅 2개 신설**(`block-branch-creation.sh`, `block-worktree-tool.sh`) → `.claude/settings.json`(팀 공유)에 기존 훅 유지한 채 추가. 8개 테스트 케이스 검증. 이미 켜져 있는 세션엔 `/hooks`를 열거나 재시작해야 반영됨. 이 정책을 CLAUDE.md에 문서화.

## 4. 마감일 3개 확정 (사용자 확인)
- **7/9**(팀 내부 목표) / **7/12**(자기주도형 학습비 지원 1차 신청 + 최학열 멘토님께 기획서 제출) / **7/14**(기획서 최종 제출) — 셋 다 별개. `schedule.md`·`open-issues.md`·PLAN.md Phase 9에 반영.
- `ideas/manager-human-orchestration.md` 신규 기록(회의록에만 있던 매니저 오케스트레이션 아이디어).
- `port_051/hermes-deep-dive.md`, `mattermost-api-deep-dive.md` 작성·커밋(Explore/조사 에이전트 결과 정리).

## 5. Hermes 게이트웨이 운영 이슈 (개인 실습)
- 슬랙 봇 무응답 원인 = 게이트웨이 프로세스가 죽어 있었음(절전/네트워크 단절). **launchd 서비스로 등록해 재부팅/로그인 시 자동 재기동**.
- 봇이 멘션 없이 제멋대로 답하던 문제: 코드 패치 없이 `strict_mention` 옵션으로 해결(채널 스레드에서 매 메시지 명시적 멘션 필요, DM 로직엔 영향 없음).
- **OpenRouter 무료 모델(`nvidia/nemotron-3-super-120b-a12b:free`) 일일 한도(50회/일) 소진** → 리셋 7/9 오전 9시. 상시 서비스엔 무료 모델 한도가 병목이 됨을 재확인.
- 개인 구글 캘린더에 자연어로 일정 등록 정상 동작 확인(7/13 버디프로그램 상담).

## 남은 질문 / 이월
- port_051 반영은 그날 대부분 끝났으므로 이 요약에서 별도 추가 없음 — 상세는 `decisions.md`/`open-issues.md`(2026-07-08 항목)와 PLAN.md가 최신 기준.
