## [turn] 2026-07-08 01:38:10

### user
어짜피 팀 지식이면, 그채널에 있던 팀원만 그 채널에 접근하는게 당연하지.

### assistant


맞아요, Mattermost 자체 권한 구조에서는 그게 기본입니다 — 비공개 채널은 원래도 그 채널 멤버만 볼 수 있어요.

문제는 **우리가 새로 만드는 팀 지능 레이어**입니다. 이 레이어가 "팀이 아는 것"을 요약해서 하나의 지식 뭉치로 만들어놓고, 누가 `@ai`에게 물어보면 그 지식 뭉치에서 찾아 답해주는 구조잖아요. 이때:

- **그 요약본 자체가 원래 채널의 접근 권한을 그대로 상속받지 않으면 문제가 생깁니다.** 예를 들어 비공개 채널 A(팀원 3명만)의 내용을 요약해서 "팀 지식 DB"에 넣어놨는데, 그 DB를 검색할 때 채널 A 멤버가 아닌 사람이 질문해도 답변에 섞여 나오면 — **원래 Mattermost에선 못 봤을 내용을, AI를 거쳐서 보게 되는 것**이라 새로운 구멍이 생기는 겁니다.

즉 "채널 자체 접근 권한"은 이미 Mattermost가 잘 해주고 있는데, **우리 레이어가 그 경계를 그대로 유지한 채로 지식을 검색해주느냐**가 진짜 설계 포인트입니다. 실무적으로는 요약본을 저장할 때 "원본 채널 ID + 그 채널 멤버 목록"도 같이 저장해두고, 질문자가 그 채널 멤버가 아니면 그 요약을 답변 후보에서 아예 제외하는 필터링을 팀 지능 레이어 안에 넣어야 합니다.
