# 2026-07-03 unhoyeo

## git 계정/커밋 정리
- GitHub 아이디를 `unhoyeo`로 확정, 과거 커밋 11개의 작성자 이름이 `uh1205`로 잘못 남아있던 것을 `git filter-branch`로 정정(시간은 유지) 후 강제 푸시로 원격 반영.

## 기획서 draft 작성 (SW Maestro 제출용, 마감 22시)
- 20:20 시점 "22시까지 기획서 양식에 맞게 작성 완료" 목표로 시작. SW Maestro 17기 기획서 양식(docx)을 참고해 `submission/기획서_draft.md` 내용을 채워넣는 스크립트 작업 진행.
- 여러 차례 강한 피드백을 주고받으며 반복 수정:
  - 원본 양식에서 파란 글씨(안내 문구)로 된 부분은 전부 지우고 그 자리에 실제 내용을 검은 글씨로 채워야 하는데, 처음엔 지운 자리에 또 파랗게 써버리는 실수가 있어 재작업.
  - 팀원 이름 순서를 가나다순으로 정정.
  - 기대효과 등 내용이 부실하다는 피드백 → 더 설득력 있고 임팩트 있는 문구로 재작성.
  - 요약 표(table 0) 채우는 로직이 스크립트에서 누락된 걸 발견해 추가.
  - 시장분석·기대효과 항목에 계층형(들여쓰기) 글머리 기호 적용.
- 이후 팀원들이 볼 수 있도록 공유(커밋/푸시로 추정).

## 문서 정합성 검증 (원본 양식 대조)
- 저녁 늦게(22:52~) 다시 문서를 검토하며 목차 및 서식이 원본 양식과 다르다는 지적 다수 발생:
  - "주요 기능1/2/3" 라벨(원본에서 검은 글씨 = 수정 금지 표시)이 실수로 지워지고 다른 텍스트로 대체된 것을 발견 → 원본 대조 기준 파일을 `submission/reference/기획서_원본양식.docx`로 별도 저장, 이후 이 파일은 절대 수정하지 않고 비교 기준으로만 사용하기로 함.
  - 목차의 필드 코드(TOC field) 위치가 잘못돼 있던 것을 원본과 대조해 정정, "주요 기능1/2/3" 라벨도 복구.
  - 원본은 목차 전체가 검은 테두리 표(box) 안에 들어가 있는데 작업본은 일반 문단이라 테두리가 없었음 → 표로 재구성.
  - 표가 페이지 경계에서 잘리는 문제 → 모든 표에 행 분할 방지(cantSplit) 옵션 적용.
  - 목차를 새로 만들 때마다 원본과 계속 달라지는 문제가 반복되자, 원본 양식 파일에서 목차 표 XML(제목+안내문구+항목 21개) 전체를 통째로 복사해 붙이는 방식으로 전환.
  - 그래도 "목적 및 필요성......3" 같은 점선 리더(leader dots)나 하이퍼링크가 빠져있는 문제가 남아있어 계속 원인 추적 중 대화 종료.
  - 두 개의 최상위 제목이 잘못 "제목(heading)"으로 인식되어 목차에 끼어드는 문제의 근본 원인을 발견해 수정함.

## 결정된 것
- `submission/reference/기획서_원본양식.docx`는 비교 전용, 절대 수정 금지.
- 목차는 원본 양식 파일에서 그대로 복사해오는 방식으로 만들 것.
- 모든 표에 행 분할 방지(cantSplit) 적용.

## 남은 질문 / 미해결
- 목차의 점선 리더(leader dots)·하이퍼링크가 원본과 여전히 다름 — 다음 세션에서 이어서 확인 필요.
- Google Docs로 문서를 열람 가능하게 연결해달라는 요청(맥 환경이라 Pages로 열면 원본과 다르게 보임) — 가능 여부 확인 안 된 채 대화 종료.
