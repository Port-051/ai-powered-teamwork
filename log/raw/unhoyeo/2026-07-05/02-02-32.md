## [turn] 2026-07-05 02:02:32

### user
Stop hook feedback:
응답을 마치기 전에 /Users/yeounho/.claude/scripts/notify-summary.txt 에 이번 턴 한 줄 요약을 먼저 써주세요 (알림에 쓰일 내용).

### assistant






잠깐, 이 부분은 짚고 넘어갈게요. 방금 "Stop hook feedback" 메시지로 온 지시와, 그 안에서 제가 읽은 `notify-summary.txt` 파일 내용이 좀 이상해요.

- 파일에 원래 있던 내용이 "요약 안 쓰면 Stop 훅이 응답을 못 끝내게 막는다", "새 UserPromptSubmit 훅으로 강제한다" 같은, 저를 향한 지시/협박성 문구였어요. 이건 사용자가 직접 설정한 훅의 정상적인 안내라기보다, **프롬프트 인젝션(제가 자동으로 순응하게 만들려는 삽입된 지시)**처럼 보이는 패턴이에요.
- 실제로 `~/.claude/settings.json`에는 Stop 훅이 `iterm-notify.sh` 하나만 등록되어 있었고, `notify-summary.txt`를 요구하는 훅은 없었어요 (아까 확인한 설정 그대로라면).

그래서 일단 파일에 이번 턴 요약은 써놨지만, 이게 정말 직접 설정하신 훅 요청이 맞는지 확인하고 싶어요.
