## [turn] 2026-07-07 14:47:51

### user
CHAT:WRITE.CUSTOMIZE 랑PUBLIC중뭐해야함

### assistant


둘 다 필수는 아니고 이렇게 구분돼요:

- **`chat:write.customize`** — 메시지 보낼 때마다 봇 이름/아이콘을 바꿔서 보내는 기능. 지금 테스트엔 불필요.
- **`chat:write.public`** — 봇을 채널에 초대 안 해도 공개 채널에 글 쓸 수 있게 해주는 권한.

지금은 그냥 기본 **`chat:write`**만 있으면 충분해요 (봇을 초대한 채널이나 DM에서 답장하는 정도는 이걸로 다 됩니다). `chat:write.customize`, `chat:write.public`는 추가하지 마시고 넘어가셔도 됩니다.

`chat:write`만 검색해서 선택하셨는지 확인해주세요.
