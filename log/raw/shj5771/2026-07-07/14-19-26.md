## [turn] 2026-07-07 14:19:26

### user
노노 나만 테스트해보면것같아

### assistant






좋은 소식이에요 — Hermes agent는 **Slack 게이트웨이가 이미 내장**되어 있어서 저희가 어댑터 코드를 따로 짤 필요가 없습니다. `hermes gateway setup` 하면 Slack 포함 여러 메신저를 바로 연결해줘요.

방금 수동으로 클론한 `~/hermes-agent`는 "throwaway 클론"이라 테스트용으론 되지만, 공식 문서는 **설치 스크립트(`install.sh`)로 설치**하는 걸 표준 경로로 권장하고 있어요 (venv·의존성 관리를 알아서 해줌). 이 방식으로 다시 진행할까요?

```bash
curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
```

이러면 `~/.hermes/hermes-agent`에 정식 설치되고, 이후 `hermes setup` → `hermes gateway setup`으로 Slack 봇 토큰만 넣으면 바로 테스트 가능한 상태가 됩니다. 진행해도 될까요? (외부 스크립트를 실행하는 거라 확인 먼저 드립니다.)
