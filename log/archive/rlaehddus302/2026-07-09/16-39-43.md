## [turn] 2026-07-09 16:39:43

### user
그러면 일단 크레덴셜은 hermes도 일단 ai 기반이니까 hermes에서 지원해 주는 것이 있을 거 아냐 hermes에서도 ai 연결을 하잖아. hermes에서 기본적으로 지원하는 무료 ai 모델들이 있지 않나?

### assistant


Hermes 소스의 `PROVIDER_REGISTRY` 전체를 보고, 기본 제공되는 무료 옵션이 있는지 직접 확인하겠습니다.









`check_nous_free_tier`라는 함수가 실제로 있네요 — Nous Portal 자체에 진짜 무료 티어가 있는지 확인해보겠습니다.













확인했습니다. **있긴 있는데, 기대하시는 수준은 아닙니다.** 정확히 짚어드리겠습니다.

## Hermes 자체엔 "기본 탑재된 무료 AI"가 없습니다

`hermes_cli/auth.py`의 `PROVIDER_REGISTRY`를 통째로 열어보니 **20개가 넘는 LLM 공급자**가 등록돼 있는데(Anthropic, OpenAI, Gemini, DeepSeek, xAI, Kimi, MiniMax 등등), **전부 다 "사용자가 직접 API 키나 로그인을 가져와야 쓸 수 있는 것"**입니다. Hermes 설치만 하면 아무 설정 없이 바로 대화할 수 있는 공짜 모델은 이 목록 어디에도 없습니다.

## 다만 딱 하나, Hermes 개발사(Nous Research) 자체 서비스에 진짜 무료 티어가 있습니다

`hermes_cli/models.py`를 보면 **Nous Portal**(Hermes를 만든 회사가 직접 운영하는 결제대행 서비스, 지난번 설명한 그것)에 `is_nous_free_tier()`, `partition_nous_models_by_tier()` 라는 코드가 실제로 있습니다. 동작 방식:

```python
def _is_model_free(model_id, pricing):
    """이 모델의 프롬프트+완성 가격이 둘 다 0원이면 True"""
```

즉 Nous Portal에 로그인만 하면(무료 가입), **그 순간 Nous가 취급하는 모델 중 가격이 $0인 것들만 골라서** 무료로 쓸 수 있게 해줍니다.

**중요한 함정**: 이 "$0짜리 모델"이 뭔지는 코드에 고정돼 있지 않고, **Nous Portal 서버가 그때그때 가격표를 내려주는 대로 결정**됩니다(`get_nous_portal_account_info()`). 그리고 Claude·GPT·Gemini 같은 우리가 원하는 프론티어 모델들은 **Nous가 뒤에서 OpenRouter를 거쳐 실비로 사오는 것**이라 절대 $0이 될 수 없습니다. 무료로 뜨는 건 보통 **오픈소스 계열의 저성능/구형 모델**들입니다 — 팀 협업 지능에 쓰기엔 품질이 부족할 가능성이 높습니다.

## 그래서 실질적 결론

| 방법 | 무료인가 | 모델 품질 |
|---|---|---|
| Hermes 기본 탑재 무료 모델 | 없음 | - |
| Nous Portal 무료 티어 | 무료 | 저성능/구형 오픈소스 모델 위주 (추정) |
| **Google Gemini API 무료 티어** (지난번 확인) | 무료 | **Gemini 2.5/3 Flash — 실사용 가능한 수준** |

**Nous Portal 무료보다 지난번 확인한 Google Gemini API 무료 티어가 여전히 더 현실적인 선택지입니다** — Nous 무료는 "그때그때 남는 저가 모델"이라 품질이 들쭉날쭉하고 우리가 통제할 수 없는 반면, Gemini API 무료 티어는 **Google이 못박은 고정 스펙(Flash 모델, 하루 1,500건)**이라 우리 서비스 설계에 예측 가능하게 넣을 수 있습니다.
