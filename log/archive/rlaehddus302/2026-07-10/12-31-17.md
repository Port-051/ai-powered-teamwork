## [turn] 2026-07-10 12:31:17

### user
아니 근데 내가 했을 때는 - 멀티플렉싱(default 게이트웨이 하나로 다 서빙) 방식에서는 Mattermost ALLOWED_USERS 같은 사용자 인증 필터링이 secondary profile(second)에는 안 먹힘 안 먹힌다고 했는데 먹히긴 했거든 그러니까 default하고 연결된 것을 second 봇에서 실행하면 막히는 식으로 해서 먹히긴 했는데 restart를 하니까 이게 다 무시되는데 뭐지?

### assistant










방금 하신 관찰 덕분에 제가 앞서 드린 설명을 한 군데 정정해야겠습니다 — "profile별로 아예 안 먹힌다"가 아니라, 소스를 더 보니 **더 구체적인 원인**이 있었습니다.

**정확한 원인**: 멀티플렉싱 모드에서 프로세스 전역 `os.environ`에는 **오직 default profile의 `~/.hermes/.env`만** 로드됩니다(`gateway/run.py` 1279번 줄, 프로세스 시작 시 한 번). `second`처럼 secondary profile의 `.env`는 이 전역 환경에는 절대 안 실리고, 격리된 contextvar 스코프(`get_secret()`)에만 들어갑니다.

그런데 `_is_user_authorized()`의 `MATTERMOST_ALLOWED_USERS` 체크는 이 격리된 스코프가 아니라 그냥 `os.getenv("MATTERMOST_ALLOWED_USERS")`를 씁니다. 그 말은:

- 이 값은 **어느 봇(default든 second든)으로 메시지가 오든 상관없이 항상 default profile의 `.env`에 적힌 값 하나**만 봅니다.
- 그러니까 처음에 "막히는 게 먹혔다"는 건 사실 `second`의 설정이 적용된 게 아니라, **그 시점에 default의 `.env`에 마침 원하시는 사용자 ID가 들어있어서** 우연히 맞아떨어진 거였을 가능성이 높습니다.
- 그 뒤 `restart`하면서(혹은 그 사이에 default의 `.env`를 건드리셨다면) default `.env`에 그 값이 비어있거나 없어진 상태로 다시 로드되니, 이제 `os.getenv`가 빈 문자열을 반환 → "허용 목록 없음"으로 처리 → 필터링이 사라진 것처럼 보이는 겁니다.

즉 핵심은: **멀티플렉싱 모드에서 Mattermost 사용자 허용 목록은 profile별로 따로 걸 수 없고, default profile의 `.env`에 있는 값 하나가 전체(모든 profile의 Mattermost 봇)에 공통 적용됩니다.** `second`의 `.env`에 뭘 적어놔도 이 인증 체크는 절대 안 봅니다.

지금 상태 확인해보시겠어요? `~/.hermes/.env`(default profile) 안에 `MATTERMOST_ALLOWED_USERS`가 지금 뭐라고 적혀있는지 보시면 (아마 비어있거나 원하시는 값이 아닐 겁니다) 이 설명이 맞는지 바로 확인되실 거예요.

**결론은 똑같습니다**: 이 문제를 제대로 해결하려면 `second`를 멀티플렉싱에서 빼서 독립 프로세스로 돌려야 합니다 — 그래야 `second`의 `.env`가 그 프로세스만의 `os.environ`으로 실제로 로드돼서 자기 자신의 `MATTERMOST_ALLOWED_USERS`가 정상 적용됩니다.
