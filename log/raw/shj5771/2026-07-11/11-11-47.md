## [turn] 2026-07-11 11:11:47

### user
2로 가고, 어짜피 dm인지 전체채팅방 대화인지 mattermost db에 분리되어서 저장 되니까, 그걸 가지고 우리 레이어에 구현하면되지않나 팀 지식 공유 베이스를만들면되지않나? 근데, 혹시 팀 지식 공유 베이스레이어를 만드는과정 자체가 hermes에서 지원하거나 쉽게할수있는방법이 이미있나?

### assistant


좋은 질문이에요, 확인해볼게요 — Hermes가 이런 플러그인 만들 때 쓸 수 있는 도구나 참고할 기존 코드가 있는지 봅니다.















확인했어요, 그리고 홍재님 말씀이 맞아요 — `chat_type`은 우리가 추측하는 게 아니라 **Mattermost가 자기 DB에 이미 구분해서 저장해둔 실제 채널 타입(D/G/P/O)을 Hermes가 그대로 넘겨주는 것**이라, "휴리스틱"이라기보다 "Mattermost의 진짜 데이터를 그대로 쓰는 것"에 가까워요. 옵션 2로 가는 게 맞는 선택이네요.

그리고 두 번째 질문 — Hermes 공식 개발자 가이드(`memory-provider-plugin.md`)를 확인해보니 꽤 잘 갖춰져 있어요:

- **정식 문서·체크리스트 있음**: 디렉토리 구조, 필수 메서드 목록이 문서화돼 있어요. 확인해보니 제가 짜려던 설계에 **`get_config_schema()`/`save_config()`가 빠져있었네요** — 필수 항목인데, 우리는 API 키가 필요 없는 로컬 파일 방식이라 빈 스텁만 추가하면 돼요.
- **참고할 기존 구현체 있음**: Hermes에 이미 8개 memory provider(Honcho, Mem0 등)가 번들로 들어있어서, 패턴을 그대로 참고 가능
- **공식 테스트 방법 있음**: `MemoryManager`를 직접 띄워서 테스트하는 패턴이 문서화돼 있음 — 제가 짜려던 수동 스크립트를 이 패턴 위에 얹으면 됨
- **배포 도구는 있는데 우리 상황엔 안 맞음**: `hermes plugins install <git주소>`로 설치하는 기능이 있긴 한데, 이건 "플러그인 하나 = 저장소 하나" 구조를 전제해요. 우리는 지금 이 레포(ai-powered-teamwork) 하나로만 다 관리하는 방식(팀원 git 경험 적어서 일부러 그렇게 함)이라, 별도 레포로 쪼개는 것보다 지금 계획한 대로 **심볼릭 링크가 이 팀 상황엔 더 맞아요.**

계획 파일에 이 내용(스텁 메서드 추가, 공식 문서 참고) 반영해서 업데이트할게요.























이제 실제 코드를 만들게요.













































Now the plugin manifest, README, design decision doc, decisions.md entry, and verification script.





















Now let's run the verification script to confirm it actually works.





검증 통과했어요 — DM/비공개채널/누락값 전부 새지 않고, 공개 채널 대화만 저장되는 것 확인했습니다. 이제 설계 결정 문서를 씁니다.



Now let's add the one-line entry to `decisions.md`.











코드·문서 작업이 끝났어요. 최종 확인해볼게요.







Good, `__pycache__` is already gitignored. Everything's implemented and verified. Here's what got built:
