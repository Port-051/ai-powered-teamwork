## [turn] 2026-07-04 15:45:06

### user
<task-notification>
<task-id>afe03830513773ccf</task-id>
<tool-use-id>toolu_01RRprx523KjjtAtxCxiuyiT</tool-use-id>
<output-file>/tmp/claude-1000/-home-shj5771-ai-powered-teamwork/a4cacc79-475c-464f-88fe-04d4871275f3/tasks/afe03830513773ccf.output</output-file>
<status>completed</status>
<summary>Agent "Clarify claude vs claude agents CLI commands" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>충분히 확인됐다. 결과를 정리해서 보고한다.

## 답변 (한국어, 비개발자용)

두 명령어는 서로 다른 걸 가리키는 게 맞습니다. 실제로 확인해봤습니다.

**`claude`** — 그냥 기본 명령어. 이걸 실행하면 지금 우리가 쓰고 있는 것처럼 "대화형 세션"이 하나 켜져서, 그 안에서 직접 대화하며 작업을 시킵니다.

**`claude agents`** — 실제로 존재하는 별도 서브커맨드(하위 명령어)입니다. `claude --help`로 확인해보니 이렇게 설명돼 있습니다:
&gt; "Manage background agents" (백그라운드 에이전트 관리)

즉, `claude agents`는 대화하는 명령이 아니라 **이미 실행 중이거나 완료된 "백그라운드 세션들"을 목록으로 보여주고 관리하는** 명령입니다. 예를 들어 `claude --bg`(또는 `--background`)로 세션을 백그라운드로 켜두면, 그걸 나중에 `claude agents`로 들여다보고 관리할 수 있습니다.

정리하면:
- `claude` = 새 작업을 대화로 직접 시작하는 것
- `claude agents` = 이미 떠 있는(또는 지나간) 백그라운드 작업들을 확인/관리하는 도구

**참고로 헷갈리기 쉬운 비슷한 이름들**도 있습니다 (사용자가 물어본 게 이 중 하나일 수도 있음):
1. **Claude Agent SDK** — 이건 명령어가 아니라, 개발자가 자기만의 커스텀 에이전트를 코드로 만들 때 쓰는 개발 도구(SDK)입니다.
2. **`/agents`** (슬래시 커맨드) — 대화 세션 안에서 입력하는 명령으로, 이 세션에서 쓸 수 있는 에이전트 종류(`.claude/agents/*.md`에 정의된 것들 등)를 보고 설정하는 기능입니다.
3. **Agent 도구** (지금 제가 쓰는 것) — 세션 안에서 하위 작업을 다른 에이전트에게 맡길 때 쓰는 도구입니다.

이 프로젝트(`ai-powered-teamwork`)에는 아직 `.claude/agents/` 폴더가 없어서, 지금 시점에는 `/agents`나 커스텀 에이전트 설정을 쓸 일은 없습니다. 팀에서 실제로 쓰는 건 그냥 `claude`로 이 폴더에서 세션 켜서 대화하는 방식입니다.</result>
<usage><subagent_tokens>24961</subagent_tokens><tool_uses>2</tool_uses><duration_ms>24999</duration_ms></usage>
</task-notification>

### assistant

