# 여운호 (unhoyeo) — 2026-07-09 작업 요약

이날은 크게 세 갈래였다: **SW Maestro 기획서(제출용 문서) 다듬기**, **Mattermost↔Hermes 통합 아키텍처 재검토(핵심 논의)**, **랜딩페이지 UI 버그 수정**. 그 와중에 전날치 로그 처리(teambrain-process) 중 보안 사고를 하나 발견해 대응했다.

## 1. 기획서(SW Maestro 제출용) v3 작성 → 다듬기 → 본문 정합성 수정
- `기획서_draft.md`(초안 v2), 공식 양식(`기획서_원본양식.docx`), 이미 채워둔 `기획서_TeamBrain_Port051.docx` 세 문서를 비교 → docx가 형식 완성도는 가장 앞서 있지만 PLAN.md의 최신 내용(Phase 2 타겟 세분화 등)이 어디에도 반영 안 됐음을 확인 → docx 형식을 기준으로 draft.md를 v3로 갱신, 최학열 멘토링 내용 반영.
- "우리 깃헙에 올라온 문서 전부 반영한 거냐"는 확인 질문에 솔직히 일부만 읽었다고 답하고 나머지(README, roadmap, hermes-deep-dive, 아키텍처 SVG, experiments/ideas 등)도 마저 확인 → 두 가지 누락 발견: (1) 이미 만들어진 시스템 구성도 SVG를 안 쓰고 텍스트 ASCII로 대체하고 있었음, (2) README "열린 질문"에 있는 개인화 리스크(개인 학습 정보가 팀원별로 안 나뉠 수 있음)가 기획서에 전혀 반영 안 됨 — 이건 팀 판단이 필요한 사항으로 보고함.
- 시스템 구성도를 Claude Artifact로 새로 제작(디자인 스킬 사용) → 가독성 피드백(글자 크기·자간·대비) 받아 반복 개선.
- 여러 차례 커밋·푸시 — 신홍재의 동시 작업(멘토 역할, 팀장 지정 항목)과 겹치지 않아 rebase로 정상 병합, SVG 파일 미완성 변경은 매번 stash로 보호 후 복원.
- 오후엔 기획서 본문에 "①메신저 어댑터를 우리가 직접 만든다"는 옛 서술이 최근 확정한 사실(Hermes 어댑터 재사용)과 어긋나는 걸 발견 — 본문 4곳을 고쳐 정합성 확보, 커밋·푸시.

## 2. Mattermost↔Hermes 통합 아키텍처 재검토 (이날의 핵심 논의)
- "기획 종합적으로 봐서 어떤 구조가 맞는지 재검토해달라"는 요청에 조사 문서를 다시 읽고 **Hermes 내장 어댑터를 재사용하고, 공식 확장점인 memory-provider(`agent/memory_provider.py`)에 팀지능 로직을 꽂는 방식(A안)이 기술적으로 더 타당하다**는 결론을 냄(PLAN.md엔 아직 "8월 재결정" 미확정 항목으로 남아있던 상태).
- "왜 그게 최선인지 진짜 근거로 설득해봐"라는 강한 재확인 요청을 받고 근거를 심화·문서화(`port_051/mattermost-hermes-integration-decision.md` 계열 신설).
- 개인화 문제 논의: 사용자가 "Hermes 프로파일 방식이면 사람마다 봇 계정이 따로 필요하고, 잘못 호출하면 기록이 섞일 수 있다"는 우려를 제기 → 판단은 보류한 채 방법 A/B/C/D를 전부 같은 형식으로 비교 문서화(C안이 왜 없는지도 포함해 정리).
- 김동연이 올린 문서와 교차 검증한 결과, **SOUL.md(성격)/MEMORY.md(업무 메모)/USER.md(학습된 사용자 정보)가 서로 다른 파일**임을 코드 근거로 확인 — 기존에 "MEMORY.md/USER.md가 섞인다"고 썼던 서술이 부정확했음을 정정.
- **`USER.md` 세션 스코프를 실제로 확인한 결과, 세션(발신자) 단위로 안 나뉘고 프로파일 단위로만 존재 — 레벨1(세션 격리)만으로는 "나를 학습하는 개인 AI"(주요기능3)가 0%도 성립하지 않음**이 코드로 확정됨.
- 여운호 개인 판단(팀 결정 아님, 제안 단계): (1) 7월 MVP는 **레벨1로 시작**하는 게 맞다(레벨2는 리소스 대비 비쌈, PLAN.md의 "완벽 말고 규칙 하나" 원칙과 부합), (2) 레벨2가 정말 필요해지면 **방법 D**(프로세스 N개 인스턴스)가 낫다 — B(비공식 확장점)는 업데이트마다 깨질 리스크가 크고, A는 "@ai 하나로 끝"이라는 핵심 UX와 충돌, (3) memory-provider(A안) 자체는 **8월까지 기다릴 필요 없이 지금 설계를 시작해도 된다**. 산출물(`mattermost-hermes-integration-decision.md`, `hermes-개인화-쉬운정리.md`)은 PLAN.md Phase 8·`open-issues.md`에 이미 링크·반영 완료.

## 3. 07-07 로그 처리(teambrain-process) 중 보안 사고 발견·대응
- 전날치(07-07) 로그를 처리하려다가, **신홍재의 07-07 로그에 Slack 사용자 토큰(xoxe.xoxp- 형식) 원문이 두 차례 그대로 붙여넣어졌고, 이미 GitHub 원격 main(커밋 `75fbf51`)에 push된 상태**임을 발견 — 로그 정책의 비밀키 자동 필터가 Slack 토큰 패턴은 못 걸렀던 것.
- git 히스토리에서 완전히 지우는 방안(문제 커밋 이후 127개 커밋 재작성 + main 강제 push + 팀 전원 로컬 재동기화)은 git 경험이 적은 팀에 부담·유실 위험이 너무 커서 보류 → **토큰 재발급(무효화)만으로 대응**하기로 결정, `port_051/open-issues.md`에 기록.
- **신홍재가 실제로 토큰을 재발급했는지는 아직 체크 안 된 채 남아있음** — 이 확인이 안 되면 노출된 토큰이 여전히 유효할 수 있음.

## 4. 설문조사(구글 폼) 문항 리뷰
- "회의나 채팅에서 결정 못 찾는다에 공감하시나요" 식 문항이 좋은 설문 문항인지 질문받고, 동의 척도의 사회적 바람직성 편향 문제를 지적 → 빈도·행동·결과 기반 질문으로 바꾸는 안 제안(Mom's Test 관점).
- 신홍재가 올린 `create_survey_form.gs`를 리뷰 — 신홍재가 이미 같은 방향(11번 문항)으로 일부 반영해온 상태를 확인, 나머지 5가지 피드백을 코드에 직접 반영: 15문항으로 축소(중복 문항 통합), 문항 순서 조정(베타 신청 이메일을 맨 마지막으로), 조건절 중첩 등 말투·문장 다듬기.

## 5. 랜딩페이지 UI 작업 (개인 실습/구현)
- Vite 랜딩페이지의 기능 캐러셀(기능①②③ 슬라이드) 스크롤 버그를 Playwright로 반복 검증하며 수정 — 휠 디바운스/잠금 로직, `scroll-snap` 옵션, 트랙패드 관성 처리에서 재발과 재수정을 여러 차례 반복.
- 가로 스크롤 구현 방식을 세 번 갈아엎음: JS 트랜스폼 방식 → 스크롤 컨테이너를 90도 회전시키는 트릭 → 최종적으로 **가로 캐러셀 자체를 포기하고, 다른 섹션과 동일한 단일 세로 스크롤 흐름 + 옆에서 슬라이드 인 되는 reveal 효과**로 단순화(가장 안정적이라고 확인).
- 슬랙 랜딩페이지를 참고해 카피 개선 — 문제점 섹션을 더 공감형으로, 혜택 문구를 구체화, CTA에 부담 적은 두 번째 버튼 추가. 실사용자가 없는 상태에서 거짓이 될 통계·고객 로고류는 의도적으로 배제.
- 완성된 빌드를 하나의 HTML로 묶어 Claude Artifact로 배포(한글 인코딩 깨짐 버그 발견·수정 후 재배포, 기본 비공개 상태).
- 부수적으로 iTerm2 한글 폭 깨짐 문제를 D2Coding 폰트 설치로 해결, Claude Code agents 패널의 시간 표시/정렬 기준을 claude-code-guide 에이전트로 조사(시간 값은 "마지막 상태 변경 후 경과"로 공식 문서화돼 있으나, Completed 섹션 정렬 기준은 공식 문서에 없음을 확인).

## 남은 질문 / 이월
- 신홍재의 Slack 토큰 실제 재발급 여부 확인 미완료(`port_051/open-issues.md` 체크박스로 남아있음).
- 레벨1 vs 레벨2 개인화 범위, memory-provider 8월 조기 착수 여부는 여운호 개인 의견만 나온 상태 — 팀·멘토 논의로 확정 필요.
- 기획서에 "USER.md/MEMORY.md가 팀원별로 안 나뉘는 리스크"를 예상문제로 노출할지 여부는 여전히 팀 판단 대기.
