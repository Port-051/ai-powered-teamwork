## [turn] 2026-07-09 16:46:44

### user
그럼 hermess 를 많이 쓰면, 돈을내야한다는거네?

### assistant


정확히는 "Hermes를 쓰는 것 자체"가 아니라 **Hermes가 내부적으로 호출하는 LLM(모델) API 사용량**에 비용이 붙는 거예요. 이 둘을 구분하는 게 중요해요.

- **Hermes 프로그램 자체**: 몇 명이 쓰든, 얼마나 오래 돌리든 무료입니다 (MIT 오픈소스라 라이선스비 없음). 우리 서버에서 그냥 계속 띄워둬도 그 자체로는 돈이 안 나가요.
- **모델 호출 비용**: Hermes가 답변을 만들 때마다 뒤에서 GPT/Claude/Nous 모델 같은 LLM을 호출하는데, 이 호출 하나하나에 토큰 단위로 비용이 붙어요. **많이 쓸수록(대화를 많이 할수록) 이 비용이 늘어나는 거예요.**

그래서 "Hermes를 많이 쓰면 돈 내야 하나"는 맞는 말이긴 한데, 정확히는 "**팀원들이 AI랑 대화를 많이 할수록 우리가 붙인 모델 API 요금이 늘어난다**"는 뜻이에요. Nous Portal 구독은 그 요금을 내는 방법 중 하나(편의 서비스)일 뿐이고, 우리는 더 저렴한 다른 모델(오픈소스 모델 자체 호스팅, 저가 API 등)을 골라서 비용을 통제할 수도 있어요 — 이 부분이 PLAN.md에서 "인원 늘어도 요금 폭탄 없음"이라고 어필하려는 지점과도 연결돼요 (모델 선택권이 우리한테 있으니까요).
