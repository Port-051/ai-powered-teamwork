# CLAUDE.md

이 파일은 이 저장소(레포)에서 작업할 때 Claude Code(claude.ai/code)가 따라야 할 안내를 담고 있다.

## 프로젝트 현황

이 레포에는 현재 **기획 문서만** 있고, 실제 소스 코드는 아직 없다. 그래서 빌드/린트/테스트 명령도 없다. 구현이 시작되면 이 파일에 실제 명령어와 아키텍처 설명을 채워 넣어야 한다.

## 이 프로젝트는 무엇인가

**TeamBrain** (Port_051, SW Maestro 17기 부산) — 비개발자 팀도 평소 쓰는 팀 메신저에서 AI 봇을 멘션(호출)하기만 하면 팀의 공유 지식("팀 인텔리전스")이 자연스럽게 쌓이고, 각 팀원은 개인용 학습 에이전트도 함께 갖게 되는 협업 도구.

- `PLAN.md` — 작업 중인 기획 문서 (v0.1, 한글). 아키텍처, 라이선스 전략, 미해결 이슈에 대한 기준 문서.
- `submission/기획서_draft.md` — SW Maestro 프로젝트 기획서 초안 (이전 프로젝트 "마실"에서 피봇).
- `submission/멘토의견서_draft.md` — 멘토 의견서 초안.
- `submission/피봇팅신청서_draft.md` — 피봇팅 신청서 초안.

## 아키텍처 (계획)

시스템은 3개 층으로 구성되며, 우리가 직접 짜는 원본 코드는 가운데 층 뿐이다:

```
Mattermost (순정 설치, 자체 호스팅)              ← 비개발자 진입점, 데이터 주권 확보
        │ REST / WebSocket API만 사용
★ 우리 코드 (원본) ★
  ① Mattermost 어댑터 (멘션, 스레드, 채널 UX)
  ② 팀 공유 인텔리전스 레이어  ← 실제 핵심 novel 부분
  ③ 개인 ↔ 팀 지식 브릿지 (권한 / 비밀정보 관리)
        │
Hermes (포크, MIT 라이선스)                       ← 개인별 학습 엔진, 세션 간 기억 유지
```

설계 원칙: 개인 에이전트 엔진(Hermes)이나 메신저(Mattermost)를 새로 만들지 않는다 — 이 둘을 연결하는 "팀 협업 인텔리전스" 레이어만 새로 만든다.

## 반드시 지켜야 할 법률/라이선스 제약

아래는 단순 참고 사항이 아니라 *코드를 어떻게 작성하느냐*를 규정하는 필수 제약이다 — 어기면 AGPL상의 의무가 발생한다:

1. **Mattermost 서버 소스코드를 절대 수정하지 않는다.** 공개된 REST/WebSocket API로만 연동한다. Mattermost 소스를 수정해서 배포하면 AGPL §13의 소스 공개 의무가 발생하는데, 이를 피해야 한다.
2. **우리의 원본 레이어는 반드시 별도 프로세스로 실행한다.** Mattermost 플러그인이나 내장 모듈 형태는 안 된다. 플러그인 형태는 파생 저작물로 간주될 위험이 있고, 그러면 우리 IP가 AGPL의 적용을 받게 된다. 공개 API를 호출하는 별도 프로세스로 두면 우리 코드를 독립적으로 라이선싱할 수 있다.
3. **제품명/브랜딩/마케팅에 "Mattermost"나 "Hermes" 상표를 쓰지 않는다.** "X를 기반으로 만들었다" 같은 사실 서술은 괜찮지만, 이름/브랜딩에 쓰는 것은 안 된다.
4. **Hermes의 MIT 저작권 고지는 포크/vendoring한 곳 어디든 반드시 유지한다.**
5. Hermes의 의존성을 추가/업데이트하기 전에는 매번 라이선스에 카피레프트(copyleft) 이슈가 없는지 다시 확인한다.

전체 논리와, 위 조건들만 지키면 유료/호스팅형(SaaS) 서비스도 AGPL상 문제없는 이유는 `PLAN.md` Phase 6 참고.

## 아키텍처를 확정된 것으로 가정하기 전에 확인할 미해결 결정 사항

(`PLAN.md` Phase 8 기준 — 최신 상태인지 먼저 확인하고 참고할 것)

- 내부용 도구로 쓸지, 외부에 파는 제품으로 만들지 — 아직 미정. 온보딩 UX 등 우선순위에 영향.
- 메모리 저장 방식: Mattermost DB만 쓸지, 검색/요약 레이어를 추가할지.
- AI를 계속 호출하는 데 드는 비용 전략 (저가/오픈모델 혼용 논의 중).
- Hermes 자체 의존성에 대한 전체 라이선스 감사는 아직 안 함.

## 팀 협업 가이드 (git 초보 팀 전용)

팀원 4명(여운호·김동연·신홍재·김선만) 모두 git/GitHub 사용 경험이 많지 않다. 이 레포에서 작업하는 Claude(누가 켜든)는 아래를 항상 지킨다:

- **용어는 쉽게, 하지만 숨기지 않는다.** git/GitHub 관련 전문 용어(커밋, 푸시, 브랜치, 워크트리 등)를 쓸 때는 처음 나올 때 괄호로 짧게 풀어준다.
  - 예: 커밋(작업 내용을 저장 시점으로 기록하는 것), 푸시(내 컴퓨터의 기록을 팀 공용 저장소로 올리는 것), 워크트리(작업 내용이 안 섞이게 임시로 복사해서 작업하는 방식)
- **실제로 저장/공유되는 작업은 항상 먼저 확인받는다.** 커밋·푸시처럼 되돌리기 번거로운 작업을 하기 전에는 "무엇을 하려는지 → 왜 필요한지 → 진행해도 될지"를 짧게 물어보고 답을 받은 뒤에 실행한다. 자동으로 진행하지 않는다.
- **고급 기능이 실패하면 조용히 우회한다.** 워크트리 같은 고급 기능이 실패하거나 이 프로젝트 규모에 안 맞아 보이면, 어려운 원인을 길게 설명하지 말고 "이 폴더에서 바로 작업할게" 하고 넘어간 뒤 이유는 한 줄로만 남긴다.
- **되돌리기 어려운 명령은 항상 확인 후 실행.** 강제 push, 브랜치 삭제, `git reset --hard`, `git clean` 등은 절대 그냥 실행하지 않고, 쉬운 말로 설명한 뒤 명시적으로 진행해도 되는지 물어본다.
- 이 팀은 SW Maestro 멘토(김선만) 의견에 따라 "이 폴더에서 Claude Code를 켜고 작업하는 것"만 협업 방식으로 쓴다 — 별도의 git 클라이언트나 터미널 명령을 팀원이 직접 익힐 필요가 없게, Claude가 그 부분을 대신 처리한다.

## 작업 로그 (log/) 자동화 정책

이 프로젝트 자체를 "AI를 활용한 팀 업무 실습"으로도 쓴다. 그래서 모든 대화 입출력을 기록으로 남긴다.

이 정책은 설명이 아니라 실제 **Claude Code hooks**(`.claude/settings.json` + `.claude/hooks/*.sh`)로 구현되어 있다. 팀원이 이 폴더에서 Claude Code를 켜기만 하면 아래가 자동으로 돈다.

- **누가 쓰고 있는지 판별**: 별도 매핑표 없이, 그때그때 `git config user.name` 값을 그대로 폴더 이름으로 쓴다.
  - `git config user.name`이 비어 있으면, `.claude/hooks/pre-prompt-pull.sh`가 프롬프트 처리를 막고 먼저 `git config user.name "이름"`을 설정해달라고 안내한다.
- **작업 시작 전**: `UserPromptSubmit` 훅(`pre-prompt-pull.sh`)이 매 프롬프트 처리 전에 항상 `git pull`을 먼저 실행한다.
- **응답이 끝난 뒤**: `Stop` 훅(`stop-log-push.sh`)이 방금 턴의 대화(마지막 사용자 입력 + Claude 응답)를 새 파일로 저장하고, 그 파일만 자동으로 `git add` → `git commit` → `git push`한다.
- **로그 구조**:
  ```
  log/
    raw/<git user.name>/<YYYY-MM-DD>/<HH-MM-SS>.md      ← 프롬프트 1턴 = 파일 1개. 가공 없이 그대로.
    processed/<git user.name>/<YYYY-MM-DD>.md           ← teambrain-process 스킬이 하루치 raw를 정리해서 넣는다 (자동 push 대상 아님)
    archive/<git user.name>/<YYYY-MM-DD>/<HH-MM-SS>.md  ← processed로 소비된 raw 원본을 같은 경로 구조로 이동
  ```
  raw의 날짜 폴더가 남아 있다는 것 자체가 "아직 processed로 안 옮겨진 것"이라는 표시가 된다. 날짜 폴더 단위로 옮기기 때문에 여러 팀원이 동시에 써도 충돌이 안 난다.
- **자동 커밋/푸시는 `log/raw/<git user.name>/` 파일에 한해서만** 매 프롬프트가 끝날 때마다 확인 없이 자동으로 실행된다.
- **그 외 전부** (코드, `log/processed/`, `log/archive/`, `port_051/`, PLAN.md 등 문서, 그 밖의 모든 파일)는 위 "실제로 저장/공유되는 작업은 항상 먼저 확인받는다" 규칙을 그대로 따른다 — 절대 자동으로 커밋/푸시하지 않는다.
- **일일 처리(raw → processed → archive)**: `/teambrain-process` 스킬로 수행한다. 팀원 전체의 `log/raw/*/*/` 를 훑어서 사람별 요약을 `log/processed/<user>/<날짜>.md`에 쓰고, 팀 전체가 알아야 할 중요한 내용은 `port_051/`에 추출·가공해 넣은 뒤, 처리에 쓰인 raw 폴더를 통째로 `log/archive/<user>/<날짜>/`로 옮긴다. (이 스킬이 만든 변경사항의 commit/push는 일반 규칙대로 확인 후 진행.)
