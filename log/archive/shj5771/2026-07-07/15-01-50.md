## [turn] 2026-07-07 15:01:50

### user
@데모앱 하고 안녕 했는데 답장이안옴 30초경과

### assistant


터미널에서 로그 확인해볼게요.





원인 찾았어요 — 봇 토큰에 스코프가 부족해요. `channels:read`, `groups:read`, `mpim:read`, `im:read` 이 네 개가 더 필요합니다.

Slack API 설정 페이지(OAuth & Permissions)에서:
1. **"OAuth 범위 추가"** 눌러서 아래 4개 추가:
   - `channels:read`
   - `groups:read`
   - `mpim:read`
   - `im:read`
2. 다시 페이지 위로 올라가서 **"Reinstall to Workspace"** (또는 워크스페이스 재설치) 클릭 → 승인

토큰 값(xoxb-)이 재설치해도 똑같이 유지될 수도, 새로 바뀔 수도 있어요 — 바뀌면 `.env`도 새 값으로 업데이트해야 하니, 재설치 후 상단 Bot Token이 이전과 같은지 확인해주세요. 다 하시면 알려주세요, 게이트웨이 재시작하는 것까지 도와드릴게요.
