## [turn] 2026-07-10 21:54:37

### user
Failed to run image. (HTTP code 500) server error - failed to set up container networking: driver failed programming external connectivity on endpoint admiring_shannon (f8f3f1c95918201f2df3a50460e34983b01e76b758e8d4f27024abb92585109f): Bind for 0.0.0.0:8065 failed: port is already allocated

설명

### assistant


이 에러는 도커(Docker)가 컨테이너를 띄우려는데, 컴퓨터의 8065번 포트(Mattermost가 기본으로 쓰는 포트)를 다른 무언가가 이미 쓰고 있어서 못 붙인다는 뜻이다.

주로 이런 상황에서 발생한다:
- Mattermost 컨테이너를 이미 한 번 실행해놨고, 그게 아직 켜져 있거나 (중복 실행 시도)
- 이전에 실행했던 컨테이너가 완전히 종료되지 않고 포트를 물고 있는 상태로 남아있거나
- 다른 프로그램이 우연히 같은 8065 포트를 쓰고 있거나

먼저 뭐가 포트를 쓰고 있는지 확인해볼게. 아래 명령을 실행해도 될까? (조회만 하는 명령이라 안전함)



이 WSL 환경에서는 지금 `docker` 명령을 못 찾는다 — Docker Desktop의 WSL 통합(integration)이 이 배포판(distro)에 안 켜져 있어서다. 즉 방금 컨테이너 실행은 아마 Windows 쪽 Docker Desktop이나 다른 터미널에서 하신 것 같다.

그래서 포트 충돌 여부는 여기서 바로 확인이 안 되고, 아래 중 하나로 확인해야 한다:

1. **Docker Desktop 앱**에서 Containers 탭 열어서 이미 떠 있는 Mattermost 컨테이너(또는 이름이 겹치는 다른 컨테이너)가 있는지 확인
2. 또는 Windows PowerShell/터미널에서 직접:
   ```
   docker ps -a
   ```
   실행해서 8065 포트를 쓰고 있는 컨테이너를 찾기

이미 같은 Mattermost 컨테이너가 떠 있다면 새로 실행할 필요 없이 그걸 그대로 쓰면 되고, 죽었는데 안 지워진 컨테이너라면 지워야 한다(`docker rm <컨테이너이름>`). Docker Desktop 창을 열어서 Containers 목록에 뭐가 보이는지 알려주면 다음 단계를 안내해줄게.
