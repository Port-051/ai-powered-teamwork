## [turn] 2026-07-07 16:45:20

### user
아니 그럼 내가 설치할 때 무슨 메세지 앱하고 연결할 건지 묻던데 이건 뭘로 한 거지?

### assistant


그건 지금 WSL에 설치한 것과는 다른 얘기입니다 — 방금 여기 WSL 설치는 `--skip-setup` 옵션으로 진행해서, 메신저 연결을 묻는 질문 자체가 아예 안 떴습니다(그 마법사를 건너뛴 거라서).

그러니 그 질문을 받으신 건 **이전에 PowerShell(윈도우 쪽)에서 설치하실 때** `hermes setup` 마법사가 물어본 것일 텐데, 그건 별도의 환경이라 제가 그때 뭘 선택하셨는지 볼 방법이 없습니다.

확인하는 방법:
- PowerShell 쪽에서 `hermes config` 또는 `hermes gateway list` 실행 — 지금 연결 설정된 플랫폼이 뭔지 보여줌
- 또는 그냥 `hermes setup`을 다시 실행해서 처음부터 다시 선택(덮어쓰기 가능)

지금 WSL 쪽은 아직 메신저 연결을 아무것도 안 한 깨끗한 상태이니, 여기서부터 새로 골라도 됩니다 — Mattermost로 바로 연결해볼지, 아니면 CLI로 먼저 대화만 해볼지 어느 쪽으로 진행할까요?
