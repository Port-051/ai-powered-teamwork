## [turn] 2026-07-07 15:11:49

### user
 Claude 구독이 있으시면 hermes setup model에서 Anthropic으로 바꾸는 게 더 빠를 수 있어요

어느 쪽으로 하시겠어요?이게낫겠다. claude구독되어있어

### assistant


좋아요, 그럼 이렇게 진행해주세요.

1. 터미널에서:
```bash
hermes setup model
```
2. 목록에서 **"Anthropic (Claude models via API key or Claude Code)"** 선택
3. 인증 방식 선택 화면이 나오면, **API 키가 아니라 "Claude Code" 로그인(OAuth) 방식**을 선택해주세요 — 이게 Claude 구독 계정 로그인으로 쓰는 방식이라, 별도 API 크레딧 결제 없이 구독으로 쓸 수 있어요 (반대로 "API Key" 방식을 선택하면 Anthropic 콘솔에서 별도로 돈을 충전해야 하는 API 과금이라 다릅니다).

화면에 뭐가 뜨는지 보여주시면 다음 단계 도와드릴게요.
