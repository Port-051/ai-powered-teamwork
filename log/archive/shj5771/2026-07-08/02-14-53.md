## [turn] 2026-07-08 02:14:53

### user
그 데이터베이스를 가지고 그럼 우리의 팀 레이어(teambrain) 로직 구현하면 끝인거네?

### assistant


큰 그림에서는 맞는데, "그것만 하면 끝"이라기엔 아직 몇 단계가 더 있어요. 정리하면:

**이미 준비된 것**:
- Mattermost 서버 (대화가 실제로 쌓이는 곳, `posts` 테이블)
- 멘션→응답 최소 뼈대 (`team-intelligence/`, Slack 버전으로 만든 것 — Mattermost용으로 어댑터만 바꾸면 됨)

**아직 안 만든 것 (이게 진짜 "teambrain 로직")**:
1. **DB에서 대화를 읽어오는 부분** — 지금은 그냥 쌓이기만 하고, 아무도 안 읽고 있음
2. **요약 추출 로직** — 아까 얘기한 "결정/진행상황/열린질문" 스키마로 뽑아내는 부분 (아직 코드 없음)
3. **요약을 어딘가에 누적 저장하는 부분** — Postgres에 테이블 추가할지, 별도 파일로 할지 결정 필요
4. **@ai가 답할 때 그 누적 요약을 컨텍스트에 넣어주는 부분**
5. **권한 필터링(DM/비공개 채널 제외)**

즉 지금 "DB 연결"은 **재료(원문 대화)가 쌓이는 통로를 뚫어놓은 것**뿐이고, 그 재료를 실제로 가공해서 "팀 지능"으로 만드는 로직(1~5번)은 아직 한 줄도 안 짜여 있는 상태예요. 이게 바로 이 프로젝트의 진짜 본체(핵심 novel 부분)입니다.

지금 이어서 1~2번(요약 추출 로직)부터 실제 코드로 시작해볼까요?
