## [turn] 2026-07-08 14:56:18

### user
<task-notification>
<task-id>a5460aa2b5f80347c</task-id>
<tool-use-id>toolu_015EEzLiaVBCsBhB1FbbBiPa</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-yeounho-ai-powered-teamwork/6c4e33ec-894a-4edb-a0c4-6537a31060c6/tasks/a5460aa2b5f80347c.output</output-file>
<status>completed</status>
<summary>Agent "Hermes 게이트웨이/어댑터 구조 탐색" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Now I have everything needed for the report.

## 1. Mattermost adapter location and REST v4 / WebSocket usage

**File:** `/Users/yeounho/.hermes/hermes-agent/plugins/platforms/mattermost/adapter.py` (1280 lines), a plugin registered via `/Users/yeounho/.hermes/hermes-agent/plugins/platforms/mattermost/plugin.yaml` + `__init__.py`.

It's a self-contained plugin (no external Mattermost SDK — pure `aiohttp`). Key points:

- Env vars read directly: `MATTERMOST_URL`, `MATTERMOST_TOKEN`, `MATTERMOST_ALLOWED_USERS`, `MATTERMOST_HOME_CHANNEL`, plus `MATTERMOST_REQUIRE_MENTION`, `MATTERMOST_FREE_RESPONSE_CHANNELS`, `MATTERMOST_ALLOWED_CHANNELS`, `MATTERMOST_REPLY_MODE`.
- REST v4: helper methods `_api_get/_api_post/_api_put` all build `f"{self._base_url}/api/v4/{path}"` and send `Authorization: Bearer {token}`. `connect()` calls `GET users/me` to authenticate and fetch bot identity; `send()` does `POST posts`; file upload via `POST files` (multipart).
- WebSocket: `connect()` spawns `self._ws_task = asyncio.create_task(self._ws_loop())`. `_ws_connect_and_listen()` builds the WS URL by regexing `http`→`ws` and connects to `/api/v4/websocket`, then authenticates over the socket itself with `{"action": "authentication_challenge", "data": {"token": ...}}` (not the header). Reconnects with exponential backoff (`_RECONNECT_BASE_DELAY=2.0` → cap `60.0`, with jitter) in `_ws_loop`, and stops retrying permanently on 401/403 (auth failures).
- Event handling: `_handle_ws_event` filters for `event == "posted"`, decodes the embedded JSON `post`, ignores its own messages and system posts, does mention-gating (see §5), downloads file attachments via `GET files/{id}` and caches them locally, then builds a `MessageEvent` and calls `self.handle_message(msg_event)` (the base class dispatch entrypoint).
- Also has a `_standalone_send()` (module-level async function) used for out-of-process cron delivery, and an `interactive_setup()` CLI wizard, and `register(ctx)` at the bottom which plugs into the plugin registry with `check_fn`, `is_connected`, `apply_yaml_config_fn`, `standalone_sender_fn`, etc.

## 2. Common adapter interface

**File:** `/Users/yeounho/.hermes/hermes-agent/gateway/platforms/base.py` (5623 lines) — `class BasePlatformAdapter(ABC)` at line 2253. Design doc: `/Users/yeounho/.hermes/hermes-agent/gateway/platforms/ADDING_A_PLATFORM.md`.

Required (`@abstractmethod`, lines ~2863-2908):
```python
async def connect(self, *, is_reconnect: bool = False) -&gt; bool
async def disconnect(self) -&gt; None
async def send(self, chat_id: str, content: str, reply_to: Optional[str] = None, metadata: Optional[Dict[str, Any]] = None) -&gt; SendResult
```
Plus per the doc, adapters are expected to also implement `send_typing`, `send_image`, `get_chat_info` (documented as "required" in practice, with stub defaults for others: `send_document`, `send_voice`, `send_video`, `send_animation`, `send_image_file`, `edit_message`, `delete_message`, `create_handoff_thread`, `send_clarify`, `send_exec_approval`).

Class attributes adapters can flip as capability flags: `supports_code_blocks`, `supports_async_delivery`, `splits_long_messages`, `typed_command_prefix`, `supports_inchannel_continuable`, `REQUIRES_EDIT_FINALIZE`.

Core shared dataclasses (same file): `MessageType` (enum: TEXT/PHOTO/VIDEO/AUDIO/VOICE/DOCUMENT/STICKER/COMMAND/LOCATION, line 1694), `MessageEvent` (line 1716 — normalized inbound message: `text`, `source: SessionSource`, `media_urls/media_types`, `reply_to_*`, `channel_context` for backfill, `metadata`), `SendResult` (line 1855). The dispatch entrypoint every adapter calls after building a `MessageEvent` is `handle_message()` (defined at line 4585), and adapters build the `SessionSource` via `self.build_source(...)` (line 5432).

## 3. Single background gateway process / multi-platform dispatch loop

**File:** `/Users/yeounho/.hermes/hermes-agent/gateway/run.py` (20,139 lines) — `class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, GatewaySlashCommandsMixin)` at line 2656. Entrypoint `def main()` at line 20041; process launcher script `/Users/yeounho/.hermes/hermes-agent/scripts/hermes-gateway`.

There is **one process** (`GatewayRunner`) that owns `self.adapters: Dict[Platform, BasePlatformAdapter]`. Startup loop (~line 6763) iterates `self.config.platforms.items()`, for every enabled platform:
```python
adapter = self._create_adapter(platform, platform_config)
adapter.set_message_handler(self._handle_message)
adapter.set_fatal_error_handler(self._handle_adapter_fatal_error)
adapter.set_session_store(self.session_store)
...
success = await self._connect_adapter_with_timeout(adapter, platform)
if success: self.adapters[platform] = adapter
```
Each adapter's `connect()` internally starts its own `asyncio.create_task(...)` for its transport loop (WebSocket listener, long-poll, etc.) — so the single event loop hosts N concurrent background tasks, one per connected platform, each calling back into the shared `_handle_message` callback registered on it. The `GatewayRunner` itself spawns further watcher tasks in the same loop: `_session_expiry_watcher`, `_kanban_notifier_watcher`, `_platform_reconnect_watcher` (auto-reconnects failed platforms — `_failed_platforms` dict), `_handoff_watcher`, `_async_delegation_watcher`, `_scale_to_zero_watcher`, `_drain_control_watcher` (all around lines 7057-7126). All routing back out to platforms goes through `gateway/delivery.py`'s router, which holds the same `self.adapters` map.

## 4. Session model

**File:** `/Users/yeounho/.hermes/hermes-agent/gateway/session.py` (2117 lines).

- `SessionSource` dataclass (line 122): identifies a message's origin — `platform: Platform`, `chat_id`, `chat_type` ("dm"/"group"/"channel"/"thread"), `user_id`, `thread_id` (forum topics/Discord threads), `scope_id`/`guild_id` (server/workspace isolation), `profile` (for multi-profile multiplexing), plus `to_dict()`/`from_dict()` for persistence.
- `build_session_key()` (line 822) is "the single source of truth for session key construction," producing keys like `agent:main:{platform}:dm:{chat_id}` or `agent:main:{platform}:{chat_type}:{chat_id}:{thread_id}:{user_id}`, e.g. for Mattermost a DM session key would be `agent:main:mattermost:dm:&lt;channel_id&gt;`. Namespace prefix (`agent:main` vs `agent:&lt;profile&gt;`) supports multi-profile gateways.
- `class SessionStore` (line 913): "Uses SQLite (via SessionDB) for session metadata and message transcripts. Falls back to legacy JSONL files if SQLite is unavailable." It initializes `from hermes_state import SessionDB`; index metadata also cached in `sessions.json` under `sessions_dir`. So session state = SQLite DB (primary) + JSONL fallback, all under the Hermes home directory (`~/.hermes/...`), keyed by the `build_session_key()` string.

## 5. `require_mention` + history-backfill-on-mention

Each platform implements mention-gating itself (there's no single generic helper — see the WhatsApp mixin exception below); Discord has the canonical backfill implementation.

**Mattermost** (`plugins/platforms/mattermost/adapter.py`, `_handle_ws_event`, ~lines 807-861): reads `MATTERMOST_REQUIRE_MENTION` (default `"true"`), `MATTERMOST_FREE_RESPONSE_CHANNELS`, `MATTERMOST_ALLOWED_CHANNELS`; builds `mention_patterns = [f"@{self._bot_username}", f"@{self._bot_user_id}"]`; drops non-DM messages lacking a mention unless the channel is in the free-response set; strips the mention text before dispatch. Mattermost has no backfill feature (comment references "OpenClaw's choice" for its 4000-char post limit but no history-fetch code).

**Discord** (`plugins/platforms/discord/adapter.py`) has the reference backfill implementation:
```python
def _discord_history_backfill(self) -&gt; bool:      # env DISCORD_HISTORY_BACKFILL, default true
def _discord_history_backfill_limit(self) -&gt; int  # env DISCORD_HISTORY_BACKFILL_LIMIT, default 50
async def _fetch_channel_context(self, channel, before, reply_target=None) -&gt; str:
    ...
    async for msg in channel.history(limit=limit, before=before, after=_after_obj, oldest_first=False):
        if msg.author == self._client.user: break   # partition point = bot's own last message
        ...
```
It scans backward up to 50 messages (line 4802: `raw = os.getenv("DISCORD_HISTORY_BACKFILL_LIMIT", "50")`), stopping early at the bot's own last message (the "natural partition point"), tags non-allowlisted senders `[unverified]`, and merges in a second scan anchored on the specific message the user replied to if any. Called from the main mention-handling block (~line 6120: `if not _is_dm and self._discord_history_backfill(): ... _channel_context = _backfill_text`). The result flows into `MessageEvent.channel_context` (defined in `gateway/platforms/base.py` line 1762) which `gateway/run.py` prepends to the prompt separately from the triggering message text.

## 6. Access control, pairing, `/platform` status, auto-pause

**Allowlists / admin control:** `/Users/yeounho/.hermes/hermes-agent/gateway/authz_mixin.py` — `class GatewayAuthorizationMixin._is_user_authorized(source)` (line 245) is the single authorization gate, checked in order: HomeAssistant/webhook bypass → upstream-relay trust → per-platform allow-bots → chat-scoped group allowlist (e.g. `TELEGRAM_GROUP_ALLOWED_CHATS`) → per-platform `{PLATFORM}_ALLOW_ALL_USERS` → adapter role-based auth (`role_authorized`) → pairing store (`self.pairing_store.is_approved(...)`) → `{PLATFORM}_ALLOWED_USERS` / `{PLATFORM}_GROUP_ALLOWED_USERS` / `GATEWAY_ALLOWED_USERS` env allowlists → adapters that self-enforce access policy (WeCom/Weixin/Yuanbao/QQBot/WhatsApp via `enforces_own_access_policy`/`dm_policy`/`group_policy`) → `GATEWAY_ALLOW_ALL_USERS` fallback → default deny. `platform_env_map` in that file maps every platform to its env var, e.g. `Platform.MATTERMOST: "MATTERMOST_ALLOWED_USERS"`, `Platform.SLACK: "SLACK_ALLOWED_USERS"`; plugin platforms not hardcoded there (e.g. Telegram covers it directly) look themselves up via `gateway/platform_registry.py`'s `allowed_users_env`/`allow_all_env` fields set at plugin `register()` time.

**DM pairing code flow:** `/Users/yeounho/.hermes/hermes-agent/gateway/pairing.py` — `class PairingStore`: `generate_code()` (line 319, 8-char code from a 32-char unambiguous alphabet, salted-SHA256 hashed at rest, rate-limited 1/10min, max 3 pending/platform), `approve_code()` (line 375, constant-time compare, lockout after 5 failed attempts for 1 hour), `is_approved()`/`revoke()`. Triggered from `gateway/run.py` (~line 8638-8677) inside the inbound-message handler: when `_is_user_authorized` fails and it's a DM with `_get_unauthorized_dm_behavior(...) == "pair"`, it calls `pairing_store.generate_code(...)` and sends the user `Here's your pairing code: {code} ... hermes pairing approve {platform} {code}`. `_get_unauthorized_dm_behavior()` (in `authz_mixin.py`, line 584) decides "pair" vs "ignore" per-platform (e.g. Email defaults to "ignore").

**`/platform` status command:** `/Users/yeounho/.hermes/hermes-agent/gateway/slash_commands.py`, `_handle_platform_command()` (line 1086) implements `/platform list|pause|resume [name]`: `list` shows `self.adapters` (connected) vs `self._failed_platforms` (retrying/paused with reason); `pause`/`resume` call into `GatewayRunner._pause_failed_platform` / `_resume_paused_platform` in `gateway/run.py` (lines 4474-4524).

**Auto-pause-on-repeated-failure:** contrary to what the name implies, current code in `gateway/run.py` explicitly disabled *automatic* pausing — `_platform_reconnect_watcher()` (~line 7570) docstring states pause/resume is available via `/platform pause|resume` "but is no longer triggered automatically — auto-pausing a recovered platform was the cause of bots silently staying dead after a transient DNS failure." The watcher retries failed platforms forever with exponential backoff capped at `_BACKOFF_CAP = 300` seconds, skipping any platform manually marked `paused`. So today the "repeated-failure" circuit breaker is operator-triggered only (`/platform pause &lt;name&gt;`), backed by the same `_failed_platforms` dict (`{config, attempts, next_retry, paused, pause_reason}`) that the reconnect watcher and the `/platform` slash command both read/write.</result>
<usage><subagent_tokens>138881</subagent_tokens><tool_uses>35</tool_uses><duration_ms>128576</duration_ms></usage>
</task-notification>

### assistant

