## [turn] 2026-07-10 10:07:17

### user
아니 그러니까 봇 자체로는 의미가 없고 그 채널에 올라오는 모든 메세지가 봇으로 흘러들어가는 구조고 그 봇하고 연결된 것이 그 메세지를 알아서 필터링해서 관리해야 한다는 건가

### assistant


네, 정확히 그렇게 이해하신 게 맞아요. 조금 더 명확히 구분하면:

## 봇 계정(Mattermost 쪽) = 그냥 "채널을 볼 수 있는 자격"

Mattermost 봇 계정 자체는 **아무 판단도 안 합니다.** 그냥 "이 채널 멤버 중 하나"일 뿐이에요. 봇이 채널에 들어가 있으면, 그 채널에 올라오는 메시지는 **그 봇 토큰으로 인증된 WebSocket 연결에 전부 흘러들어옵니다** — 멘션됐든 안 됐든 상관없이.

## 필터링/판단(Hermes 쪽) = 토큰을 쥔 프로그램이 알아서 처리

Mattermost 서버 입장에서는 "이 메시지는 봇을 위한 거고 저건 아니고"를 구분해주는 기능이 없어요. 그 판단은 전부 **봇 토큰을 갖고 연결한 쪽(Hermes 게이트웨이)이 코드로 직접** 합니다:

- `@봇이름`이 텍스트에 있는지 문자열 매칭 (`MATTERMOST_REQUIRE_MENTION`)
- 보낸 사람이 허용 목록에 있는지 (`MATTERMOST_ALLOWED_USERS`)
- 어느 채널에서 왔는지 (`MATTERMOST_ALLOWED_CHANNELS`)

이 세 가지 다 **Mattermost 서버가 걸러주는 게 아니라, 메시지를 다 받은 뒤에 Hermes가 자기 쪽에서 버릴지 말지 결정하는 것**입니다.

## 그래서 결론

"봇"이라는 게 Mattermost에서는 독립적으로 뭔가를 판단하는 존재가 아니라, **그냥 하나의 파이프(통로)**예요. 그 파이프 반대편에 뭘 연결하느냐(Hermes 게이트웨이, 그리고 그 안의 필터링 로직)에 따라 실제 동작이 결정되는 구조입니다. Slack처럼 "권한을 좁게 줘서 애초에 필요한 것만 들어오게" 하는 게 아니라 "일단 다 열어두고 받는 쪽에서 알아서 골라낸다"는 설계 방향인 거죠.
