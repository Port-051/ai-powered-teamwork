## [turn] 2026-07-10 21:34:01

### user
즉:
- Hermes가 "볼 수 있는" 건 봇이 채널에 들어와 있는 동안 실시간으로 흘러오는 메시지들뿐입니다 (WebSocket으로 구독).
- 봇을 나중에 초대하거나 재시작했을 때, 그 이전에 쌓여있던 Mattermost 대화 기록을 소급해서 읽어오는 기능이 없습니다.
- 세션/기억도 기본적으로 "채널+사용자" 단위로 나뉘어서, 지금 이 대화창에서 오간 것만 기억하지, Mattermost 전체 서버의 과거 기록을 DB 조회하듯 훑어서 답하는 구조가 아닙니다. 이 말은 봇이 채널에 들어와 있는 동안은 그냥 모든 대화를 hermes로 다 전달한다 이거지?

### assistant


정확히 확인해서 답변드리겠습니다. "다 전달한다"가 맞는지, 아니면 멘션된 것만 처리하는지는 소스 코드를 봐야 확실합니다.





코드로 정확히 확인됐습니다. 제가 앞서 "다 전달한다"처럼 들리게 말한 건 부정확했네요. 정정하겠습니다.

**아닙니다.** 봇이 채널에 있는 동안 WebSocket으로 **모든 이벤트를 수신하긴** 하지만(`_handle_post` 함수가 채널의 모든 새 글에 대해 호출됨), 그 안에서 바로 필터링이 걸립니다 — `plugins/platforms/mattermost/adapter.py:849`:

```python
if require_mention and not is_free_channel and not has_mention:
    logger.debug("Mattermost: skipping non-DM message without @mention ...")
    return
```

즉 실제 동작은 이렇습니다:

1. **채널의 모든 메시지가 봇 프로세스까지는 도착**합니다 (WebSocket 구독이라 채널에 있으면 다 들어옴).
2. 하지만 `MATTERMOST_REQUIRE_MENTION`이 기본값 `true`라서, **`@봇이름`으로 멘션되지 않은 메시지는 그 자리에서 그냥 버려집니다(`return`)** — Hermes 에이전트(LLM)에게 넘어가지도, 기억/세션에 쌓이지도 않습니다.
3. 예외는 두 가지: DM(다이렉트 메시지)이거나, `MATTERMOST_FREE_RESPONSE_CHANNELS`에 등록된 채널(멘션 없이도 전부 응답).
4. 멘션 필터를 통과한 메시지도, 그 전 채널 대화 흐름을 다 알고 답하는 게 아니라 — 세션이 "채널+사용자" 단위라 **그 발신자와의 기존 세션 맥락**만 갖고 답합니다. 멘션 안 된 다른 사람들의 대화는 애초에 필터에서 버려졌으니 알 수도 없습니다.

정리하면: **"봇이 채널에 있는 동안 오간 대화 전체를 다 기억한다"가 아니라, "멘션된 메시지(또는 DM/free-response 채널)만 골라서 처리하고, 그 외 잡담은 필터 단계에서 폐기된다"**가 정확한 동작입니다.
