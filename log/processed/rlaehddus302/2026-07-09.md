# 2026-07-09 김동연(rlaehddus302)

## Hermes "개인화" 정밀 재검증 — 세션 격리 vs 프로파일은 다른 개념 — 팀 공유 (기존 이해 보정)
- 오후(14:36)에 "Mattermost↔Hermes를 봇 하나+발신자 감지로 바로 이으면 개인화가 안 되는 거 아니냐"는 질문에서 출발. 1차 답변은 소스(`gateway/session.py`)를 근거로 "내장 Mattermost 어댑터가 `user_id=sender_id`를 세션 키에 넣어 사람별 세션(기억)을 자동 분리하니 걱정할 필요 없다"였음.
- 하지만 김동연이 "예전에 확인한 걸로는 API가 profile 분리를 지원 안 하는 걸로 아는데"라며 재검증을 요구(14:48) → 소스를 더 깊이 파본 결과 **1차 답변이 부정확했고 김동연의 기억이 맞았음**을 정정:
  - **레벨1 세션 격리(user_id 단위)**: `X-Hermes-Session-Key` 헤더/어댑터의 `group_sessions_per_user`(기본 on)로 API 레벨에서 가능. 단 분리되는 건 "대화 히스토리 + 세션 넘는 기억"뿐.
  - **레벨2 프로파일(진짜 개인 에이전트)**: `~/.hermes/profiles/<name>/`처럼 SOUL.md·skills·기억·config가 통째로 독립된 것. **api_server.py에는 요청별로 profile을 고르는 경로가 아예 없음**(grep 0건). profile 라우팅은 `webhook.py`의 `/p/<profile>/`로만 존재하고 그마저 기본 OFF + "봇(어댑터 자격증명) 하나 = profile 하나"로 묶여서, 봇 하나로 사람별 profile을 못 나눔.
- **결론/제안**: MVP의 "개인화"가 (ㄱ)사람별 기억 분리만이면 지금 방식(봇 하나 + session-key)으로 충분하지만, (ㄴ)각자 SOUL.md·skills가 따로 진화하는 진짜 개인 에이전트가 필요하면 **A안(사람마다 별도 Hermes 인스턴스를 띄우고 우리 어댑터가 발신자→인스턴스로 라우팅, 소스 무수정)**이 필요. 포크(소스 수정)는 이 문제의 해법이 아니라는 점도 재확인(MIT라 법적으로는 가능하지만 업스트림 업데이트 포기 비용이 큼).
- **아직 미정**: (ㄱ)/(ㄴ) 중 어느 쪽이 MVP 목표인지는 이 대화에서 결정되지 않음 — 팀 결정 필요.
- 15:02에 pull → 확인 후 `port_051/hermes-개인화-쉬운정리.md` 작성·push 완료. 여운호가 이미 올려둔 `mattermost-hermes-integration-decision.md`(개인화 A/B/C/D 비교표, 방법C=api_server profile 불가 결론 동일)와 중복되지 않게, "세션격리 vs 프로파일이 왜 다른가"라는 개념 설명에 집중한 컴패니언 문서로 작성.

## SOUL.md / MEMORY.md / USER.md — 용어 혼선 정정 (기존 문서 오류) — 팀 공유
- 07-08 신홍재·김동연 공동 분석(`hermes-source-analysis.md`)에서는 "AI 성격/자기모델" 파일을 MEMORY.md로 불렀는데, 같은 김동연의 다른 문서는 SOUL.md라고 불러 용어가 어긋나 있었음. 임의로 통일하지 않고 소스로 재검증(15:15~15:21) 결과, **같은 파일의 다른 이름이 아니라 실제로 역할이 다른 3개의 별개 파일**임을 확인:
  - **SOUL.md**: 사람이 직접 편집하는 고정 캐릭터 설정("이 AI는 어떤 성격/말투인가"). 설치 시 기본 문구 자동 생성(`hermes_cli/default_soul.py`), 사람이 한 번 고치면 시스템이 이후 절대 덮어쓰지 않음, 시스템 프롬프트의 "고정(stable) 계층"에 들어감. AI가 스스로 쓰는 도구는 없음.
  - **MEMORY.md / USER.md**: AI가 대화 중 자동으로 채우는 파일, 매 세션/턴마다 갱신되는 "가변(volatile) 계층".
- **기획서 문구 "AI가 나를 학습해 스스로 발전한다"는 사실 SOUL.md가 아니라 USER.md가 담당하는 개념**이며, 기존에 "프로파일 = SOUL.md+MEMORY.md+USER.md 세트"로 뭉뚱그려 쓴 표현은 부정확했다고 정정. → 07-08 분석 문서·이후 파생 문서에 이 구분을 반영할 필요 있음(`hermes-deep-dive.md`, `hermes-개인화-쉬운정리.md` 등 재확인 권장).

## 랜딩 페이지 작업 (팀 저장소 반영)
- 14:07 `landing/` 폴더 첫 push 완료(22개 파일, 빌드 캐시/`.idea/` 제외, `main` `b3bcdfc..8c18050`). GitHub `Port-051/ai-powered-teamwork`에 반영됨.
- 15:44~16:06 "기능①②③을 가로로 슬라이드"하는 UX 구현. 트러블슈팅: `/mnt/c`(윈도우 드라이브)에서 Vite HMR이 안 먹혀 브라우저가 옛 버전을 계속 보여준 게 "안 된다"는 착시의 원인이었음(강력 새로고침/시크릿창으로 해결). 이후 Windows 접근성 설정(애니메이션 끔 → `prefers-reduced-motion`으로 인식돼 가로 슬라이드가 자동으로 꺼지던 것)도 원인으로 밝혀져, 사용자 요청에 따라 모션 설정과 무관하게 "항상 가로 슬라이드" 강제로 최종 변경. 이 변경분(`FeatureDeck.tsx`, `styles.css` 등)은 이 시점까지 커밋 안 됨(세션 종료 시점 기준 미커밋 상태로 보임).

## Claude/Gemini 웹 구독 재활용 리스크 재학습 (개인 학습, 07-07 리스크 항목과 연결)
- 07-07에 이미 팀에 공유된 "Nous Portal/웹 구독 재활용 = 약관 위반 리스크"를 다시 짚어가며 이해: 정식 API 키(종량제) 호출은 안전하지만, 사람이 브라우저로 로그인해 받는 세션 쿠키를 자동화 프로그램이 빼내 계속 재사용하는 방식은 위험(계정 정지 가능) — TeamBrain은 반드시 정식 API 키 방식을 써야 함을 재확인.
- "ChatGPT는 이런 재활용을 허용해주는 게 사실이냐"는 질문을 던졌으나, 로그 상 어시스턴트가 "확인해보겠다"고 한 직후 파일이 끊겨 있어 **실제 답변(사실 확인 결과)이 기록에 없음** — 미해결 상태로 이월 필요.

## 기타 (개인/환경)
- 오전(12시경) 기획서_draft.md 병합 충돌을 "원격 우선"으로 해결, stash/CRLF 이슈 등 git 운영 트러블슈팅(팀 공유 가치 없는 반복 작업).
- 16:10 pull로 원격 신규 커밋 37개(대부분 로그 자동커밋) 반영, 작업 중이던 랜딩페이지 파일은 그대로 보존.

## 남은 질문 / 이월
- **개인화 범위 결정 필요**: MVP가 (ㄱ)사람별 기억 분리로 충분한지 (ㄴ)각자 SOUL.md/skills가 따로 진화하는 진짜 개인 에이전트(A안: 인스턴스 N개 + 라우팅)까지 필요한지 — 아직 미정, 8월 재결정 대상으로 제안된 상태.
- **SOUL.md/MEMORY.md/USER.md 역할 구분 정정을 기존 문서(07-08 분석, hermes-deep-dive.md 등)에 반영할지** 확인 필요.
- **ChatGPT가 웹 구독 재활용을 허용하는지 여부** — 질문만 던져지고 실제 확인 결과가 로그에 없음, 재확인 필요.
- 랜딩페이지 가로 슬라이드 변경분 커밋 여부 확인 필요(이 대화 시점엔 미커밋).
