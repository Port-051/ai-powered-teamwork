## [turn] 2026-07-30 09:47:58

### user
특정 날짜/시간에 슬랙으로 리마인드 알림을 보내는 기능을 만들고 싶어.

### assistant


프로젝트 현재 상태부터 확인하고, Slack 관련해서 이미 정해진 게 있는지 문서를 볼게요.







`ideas/team-calendar-notification.md`가 이미 있네요. 관련 문서 읽어볼게요.



























코드 위치와 설계 방향이 잡혔습니다. 진행 전에 한 줄 정리:

- 이 레포에는 아직 제품 코드가 없고(문서 + `landing/` + `experiments/` 실습 기록만), Slack은 팀이 실제로 봇을 붙여 본 검증된 플랫폼입니다.
- `ideas/team-calendar-notification.md`(07-04, 여운호)의 3번 항목 "일정 시작 전 자동 리마인드"가 정확히 이 요청이라, 그 아이디어의 Slack 버전 실물로 만들겠습니다.
- PLAN.md 원칙대로 **메신저와 분리된 별도 프로세스**로 만들고, 나중에 Mattermost로 갈아끼울 수 있게 전송부를 인터페이스로 분리합니다.

파이썬 환경부터 확인할게요.





환경 확인 결과: Python 3.14.6, `slack_sdk`/`slack_bolt`/`pytest` 미설치. 그래서 **핵심 경로(예약 → 시간 되면 슬랙 전송)는 표준 라이브러리만으로** 동작하게 만들고(`urllib`로 Slack API 직접 호출), 슬랙에서 봇에게 말해서 등록하는 부분만 선택적으로 `slack_bolt`를 쓰게 하겠습니다. 테스트도 `unittest`(기본 내장)로 씁니다 — 팀원이 아무것도 설치 안 해도 바로 돌아갑니다.

먼저 저장소·시간파싱·전송 계층을 만듭니다.









시간 표현 파서를 씁니다 — LLM 호출 없이 규칙 기반으로 (팀이 OpenRouter 크레딧에 막힌 이력이 있어서, 이 기능만은 비용·네트워크 없이 결정적으로 동작하게).











스케줄러와 CLI(터미널 명령), 그리고 슬랙 봇 리스너를 씁니다.















테스트를 쓰기 전에 파서가 실제로 어떻게 읽는지 먼저 눌러봅니다.









원인 찾았습니다 — `오전/오후` 앞의 공백까지 매치 범위에 들어가서 날짜 구간과 겹쳐 시각이 버려졌습니다. 그리고 "어제 3시"가 조용히 오늘로 해석되는 문제도 함께 고칩니다.



















테스트를 작성합니다 (표준 `unittest` — 추가 설치 없이 돌아가게).









시간 파서 37개 테스트 전부 통과. 나머지 계층 테스트를 씁니다.















89개 테스트 전부 통과. 이제 실제로 "예약 → 시간 되면 전송"까지 되는지 종단 확인을 합니다 (전송은 dry-run으로).













E2E 동작 확인됐습니다 (늦은 전송 표시, 멘션, 상태 갱신 모두 정상). 표시 두 곳만 다듬습니다 — 한글 폭 때문에 표가 어긋나고, `예정 ...· 등록` 사이 공백이 빠졌습니다.











README와 설정 예시 파일을 씁니다.

























테스트 92개 통과, 표 정렬도 맞습니다. 이제 문서를 실제 상태에 맞게 갱신합니다 (CLAUDE.md가 "구현 시작되면 명령어를 채워 넣으라"고 지시하고 있어서 그 부분 포함).











`slack_bolt`가 없을 때 친절한 안내 대신 원시 ImportError가 나는 순서 문제 하나를 고칩니다.
