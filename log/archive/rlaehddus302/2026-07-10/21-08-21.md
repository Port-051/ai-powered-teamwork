## [turn] 2026-07-10 21:08:21

### user
2026-07-10 21:06:23,411 WARNING gateway.channel_directory: Channel directory: failed to list Slack channels for team T0BFGPWUYQM: The request to the Slack API failed. (url: https://slack.com/api/users.conversations, status: 200)
The server responded with: {'ok': False, 'error': 'missing_scope', 'needed': 'channels:read,groups:read,mpim:read,im:read', 'provided': 'app_mentions:read,chat:write'} 는 뭐지?

### assistant




이건 Mattermost가 아니라 **Slack** 봇 쪽 워크스페이스(`T0BFGPWUYQM`)에서 발생한 로그네요.

**무슨 뜻인가**: 게이트웨이가 "채널 디렉터리"(어떤 채널들이 있는지 목록 캐시)를 만들려고 Slack API(`users.conversations`)를 호출했는데, 지금 Slack 봇 토큰에 부여된 권한(scope)이 `app_mentions:read, chat:write` 두 개뿐이라 거부당한 겁니다. 필요한데 없는 권한은 `channels:read, groups:read, mpim:read, im:read` — 즉 "어떤 채널/그룹/DM이 있는지 목록을 볼 수 있는" 권한이 하나도 없는 상태입니다.

**실제 영향**: 이건 치명적인 에러는 아니고 "채널 디렉터리 갱신 실패"만 나는 겁니다. `chat:write`, `app_mentions:read`는 있으니 멘션 받고 답장하는 기본 동작 자체는 될 수 있지만, 채널 목록 조회 관련 기능(예: 어떤 채널에 있는지 파악, 자동 채널 매핑 등)은 안 됩니다.

**해결 방법** (Slack API 사이트 → 해당 앱 설정에서):
1. https://api.slack.com/apps → 이 앱 선택 → **OAuth & Permissions**
2. Scopes에 `channels:read`, `groups:read`, `mpim:read`, `im:read` 추가
3. 페이지 상단에 뜨는 **"Reinstall to Workspace"** 반드시 클릭 (scope만 추가하고 재설치 안 하면 적용 안 됨)
4. `hermes gateway restart`

문서에도 나와 있듯, scope를 바꾸면 **반드시 재설치(reinstall)**해야 반영됩니다 — 이 부분을 놓치는 경우가 흔하다고 합니다.
