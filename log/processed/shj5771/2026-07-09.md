# 신홍재 (shj5771) — 2026-07-09 작업 요약

## 1. 오전: 기획서_draft.md 멘토 구성/역할 정리 + 시스템 구성도 갱신 (09~10시)
- 멘토 3명(강진범/김선만/최학열) 표에서 "AI 네이티브/AI 카이스트 박사/AI 전문가" 같은 유형 라벨은 빼고 주요활동만 적기로 하고, 순서를 강진범 → 김선만 → 최학열로 정렬.
- 팀 구성 표의 "팀장 지정 여부" 칸 삭제, 강진범 멘토 역할란을 "실무 적용 관점의 MVP 기능 우선순위·사용성 검증 지도"로 채움. 이 과정에서 팀원이 거의 동시에 같은 파일을 커밋해서 **pull/rebase 충돌이 두 차례** 발생(v2, v3) — 원격 버전을 베이스로 두고 빠진 항목만 다시 채워 병합.
- PLAN.md가 갱신된 걸 확인하고 시스템 구성도를 다시 그림 — Mattermost → Hermes(내장 게이트웨이) → 우리 코드 순서로, 통합 지점(A안/B안)은 아직 미정인 상태 그대로 반영.

## 2. 오전~오후: 랜딩페이지 — 팀원 버전과 충돌 발견·해소, 레이아웃 변경 (10~15시) ★팀 공유
- 어제 만든 랜딩페이지를 열어보려 했으나 `landing/` 폴더에 실제 소스(package.json, app/ 등)가 없고 `node_modules`만 남아있어 **소스가 유실된 상태**였음 → 임시로 Next.js 기반으로 로컬에 재구성해서 띄움(localhost:3411).
- 이후 rebase 도중, **원격에 팀원이 이미 다른 스캐폴딩(Vite 기반)으로 `landing/`을 새로 커밋해놓은 걸 발견**(최근 커밋: "랜딩 페이지 구조 개편 및 개인 AI 설명 강화"). 완전히 다른 프로젝트라 자동 병합이 안 돼서, 로컬 Next.js본은 버리고 팀원의 Vite 버전을 그대로 채택.
- 이후 "글이 먼저 보이고, 스크롤해서 내리면 사진이 나타나는" 형태로 3개 기능 섹션 레이아웃 변경(스크롤 reveal 애니메이션을 텍스트/이미지 개별 트리거로 분리). 중간에 "안 된다"는 항의가 있었으나 원인은 브라우저 캐시/탭 문제였고 실제로는 정상 동작.

## 3. 낮: 수요조사(설문) 설계 (11~17시) ★팀 공유
- 구글 앱스크립트로 설문을 자동 생성하는 스크립트를 작성해 `submission/` 폴더 경로에 올림(팀원 공유 목적).
- 팀원(동연) 피드백을 반영해 여러 라운드로 개선:
  - 협업 툴 선택지에 MS Teams·네이버웍스·플로우 추가, "기타" 선택 시 입력창이 안 뜨던 버그 수정, 우려사항 문항 존댓말 다듬기.
  - **"공감하시나요?" 식 동의척도 질문을 Mom's Test 원칙("가설을 그대로 물으면 다들 착하게 동의한다")에 따라 지적** — 회의/채팅이 섞여 있던 문제까지 포함해, 빈도·실제행동·결과·현재 대안을 묻는 서술형 질문 4개로 전면 교체. "최근 1개월간 몇 번 찾아봤나" 문항의 보기를 4단계(0회/1~2회/3~5회/6회 이상)로 세분화.
  - 개인화 AI 에이전트 문항이 "너무 추상적"이라는 지적을 받아 구체 예시를 넣어 수정.
  - 최종 15개 문항에 번호(1~15) 부여, 커밋+푸시 완료(랜딩페이지 WIP는 별도로 커밋 보류).

## 4. 오후: 아키텍처 재점검 — 통합 지점 A안/B안 재논의 (15~16시) ★팀 공유 (PLAN.md Phase 8 / open-issues.md 관련, 아직 미반영)
- 신홍재가 아키텍처를 "Mattermost → Hermes → 우리 레이어"(A안, PLAN.md에 그려진 순서) 순으로 설명했는데, 팀원이 "우리 레이어가 Hermes-Mattermost 사이 아니냐"(B안)고 지적.
- 논의 끝에 **"팀 전체 채널 대화를 관찰해서 지식으로 쌓는" 목표라면, Hermes 내장 게이트웨이(멘션한 개인에게만 응답하는 구조)로는 부족하고, 우리 레이어가 Mattermost API를 직접 구독해서 필요한 것만 Hermes로 라우팅하는 B안 구조가 더 맞다는 쪽으로 논거가 기움**. 이는 PLAN.md Phase 4/8에서 "미결정"으로 남아있던 A/B 선택(8월 재결정 예정)에 대한 실질적 근거 축적으로 볼 수 있음. **다만 이 결론이 PLAN.md나 open-issues.md에 아직 반영되지 않음** — 반영 여부를 물었지만 로그상 답 없이 다음 화제로 넘어감.
- B2C SaaS 메신저를 직접 개발하는 방향도 검토: 라이선스 제약(AGPL)은 사라지지만 실시간 동기화·멀티기기·푸시알림 등 인프라 개발 부담이 커지고, 무엇보다 **데이터가 자체 서버가 아닌 우리 중앙 서버에 쌓이게 되어 "데이터 주권"이라는 핵심 차별점을 잃는다**는 트레이드오프로 정리(비추천 의견 제시, 팀 차원의 최종 결정은 아님).
- MVP 스코프 제안(15:11, 논의 수준): 개인화는 방법 A(봇 계정을 팀원 수만큼)로 MVP에 우선 채택하고, 방법 B(코드 확장 기반 개인화)는 검증 리스크가 커서 고도화 단계로 보류. 프라이버시 필터는 1차 룰베이스만, 2차 AI 판별기는 다음 단계로 미루자는 제안.
- 향후 서비스 확장 시 과금모델 아이디어(논의 수준): "번들형 구독료(모델 비용을 우리가 흡수) + 선택적 BYOK 저가 플랜" 하이브리드를 추천받음 — PLAN.md 반영 여부를 물었으나 로그상 답 없음.

## 5. 기타 라이선스/기술 Q&A (14~20시)
- Hermes(MIT, Nous Research 제작)와 Mattermost(AGPL)의 라이선스 차이 재확인 — Hermes는 자유롭게 포크·수정 가능, Mattermost는 API로만 연동해야 하고 이름만 바꿔서 그대로 쓰는 것도 안 됨(이미 CLAUDE.md에 반영된 내용, 재확인 성격).
- "Mattermost 위에 TeamBrain 브랜드로 커스텀 UI/UX 클라이언트를 씌우는" 방식(백엔드는 그대로, 프론트만 새로 개발)이 가능한지 논의 — 아이디어 단계로 언급만 되고 결론은 명시적으로 안 남음.
- Hermes 유료 요소(Nous Portal 구독)와 OpenRouter API 차이 설명 — 현재는 OpenRouter 방식을 계속 쓰고 있음을 재확인(기존 방침 유지, 새 결정 아님).
- 로그 자동 push 실패("네트워크 또는 충돌") 안내 문구의 원인 설명 — 여러 팀원이 동시에 세션을 켜놓고 써서 생기는 커밋 경합이며 데이터 유실은 아님, 그 자리에서 정리 완료.

## 6. 저녁~밤: 개인 — 맥북 이전/설정 (19~23시, 팀 업무 무관)
- 윈도우에서 맥북으로 이전 준비, Claude Code 설치, 초기 설정(한영 전환, Apple ID 로그인 오류, Xcode Command Line Tools, Chrome 설치, 강제종료, 배터리 관리)까지 다수의 개인적 트러블슈팅 — 팀 공유 대상 아님, 요약에서 제외.

## 남은 질문 / 이월
- 통합 지점 A안/B안 재논의 결과("B안 쪽에 무게가 실림")가 PLAN.md/open-issues.md에 아직 미반영.
- 과금모델(번들형 구독료 + BYOK 하이브리드) 아이디어도 PLAN.md Phase 6/9에 미반영.
- **Slack 토큰 재발급 여부 — 이 날(2026-07-09) 로그 전체(121개 파일)를 확인했으나 관련 언급이 전혀 없음.** open-issues.md의 "[ ] 신홍재: Slack 토큰 재발급 확인 필요" 액션아이템은 이 날짜 기준으로 진행상황 없이 그대로 미해결 상태로 보임.
