## [turn] 2026-07-07 15:21:58

### user
야 그럼 일단 1단계 — 슬랙 앱(봇) 만들기

1. 브라우저에서 https://api.slack.com/apps 접속 (로그인 상태여야 함)
2. "Create New App" 버튼 클릭
3. "From scratch" 선택
4. App Name 입력 (예: ai-bot-test), Pick a workspace → 아까 만든 워크스페이스 선택
5. "Create App" 클릭 → 앱 관리 화면으로 이동됨

2단계 — Socket Mode 켜기 (서버 노출 없이 연습하기 위한 핵심)

1. 왼쪽 메뉴에서 "Socket Mode" 클릭
2. "Enable Socket Mode" 토글을 켬
3. 토큰 이름 입력창이 뜸 → 아무 이름(예: soc클릭
4. xapp-로 시작하는 토큰이 나옴 → 메모장 같 en)

3단계 — 봇 권한(Scope) 설정

1. 왼쪽 메뉴에서 "OAuth & Permissions" 클릭
2. 아래로 스크롤 → "Scopes" → "Bot Token Sco
3. "Add an OAuth Scope" 클릭해서 아래 2개 추
  - app_mentions:read (누가 봇을 멘션했는지
  - chat:write (메시지 보낼 권한)
4. 페이지 맨 위로 스크롤 → "Install to Workspace" 버튼 클릭
5. 권한 확인 화면 나오면 "Allow" 클릭
6. 설치 완료되면 xoxb-로 시작하는 토큰이 보임 → 복사해두기 (이게 Bot Token)

4단계 — 멘션 이벤트 구독하기

1. 왼쪽 메뉴에서 "Event Subscriptions" 클릭
2. "Enable Events" 토글을 켬 (Socket Mode라  고 그냥 켜짐)
3. 아래로 스크롤 → "Subscribe to bot events" 섹션
4. "Add Bot User Event" 클릭 → app_mention
5. "Save Changes" 클릭
6. 화면 위에 "재설치 필요" 알림 뜨면 → "rein 시 Allow

이제 토큰 2개(xoxb-..., xapp-...)가 준비됐어

5단계 — 봇을 채널에 초대

1. 슬랙 앱으로 돌아가서 아무 채널이나 열기
2. /invite @ai-bot-test 입력 (앱 이름은 4단계 상관없이 만들 때 지은 이름)

6단계 — 코드 작성 (WSL 터미널에서)

  Ran 1 shell command

WSL에 Python 3.10이 이미 있네요. 개인 연습용 폴더를 하나 만들어서 진행하면 됩니다.

mkdir -p ~/slack-bot-practice && cd ~/slack-bot-practice
pip3 install slack_bolt

bot.py 파일을 만들어서 아래처럼 작성:

from slack_bolt import App
from slack_bolt.adapter.socket_mode import SocketModeHandler

BOT_TOKEN = "xoxb-여기에-복사한-Bot-Token"
APP_TOKEN = "xapp-여기에-복사한-App-Token"

app = App(token=BOT_TOKEN)

@app.event("app_mention")
def handle_mention(event, say):                                                                                      user = event["user"]
    text = event["text"]                                                                                             say(f"안녕하세요 <@{user}>님! '{text}'
                                                                                                                 if __name__ == "__main__":
    SocketModeHandler(app, APP_TOKEN).start()                                                                    7단계 — 실행 & 테스트
python3 bot.py                                                                                                   
터미널에 ⚡️ Bolt app is running! 같은 메시지가 뜨면 성공이에요. 이제 슬랙 채널에서:                              
@ai-bot-test 안녕                                                                                                쳐보면, 봇이 바로 답장할 거예요. 이게 되면 " " 왕복을 실제로 완성한 거고, 이게 우리Mattermost 어댑터에서 만들 것과 완전히 똑같

8단계 (선택) — 진짜 AI가 답하게 해보기

say(...) 부분을 진짜 AI 호출로 바꾸면 됨. 예를 들어 Claude API를 쓰면:

import anthropic

client = anthropic.Anthropic(api_key="여기에

@app.event("app_mention")
def handle_mention(event, say):
    text = event["text"]
    response = client.messages.create(
        model="claude-sonnet-5",
        max_tokens=500,
        messages=[{"role": "user", "content"
    )
    say(response.content[0].text)

이렇게 되면 "슬랙 멘션 → 진짜 AI가 답변" 왕 리 프로젝트 MVP의 핵심(@ai 멘션 → 답
돌아옴)을 슬랙으로 미리 연습해본 게 됩니다.
이걸 내 파일 안에 md로 정리해서 만들어 줘

### assistant
















`experiments/hermes-slack/2026-07-07/dongyeon/SETUP.md`에 정리해서 만들어 뒀습니다. 내용은 보내주신 가이드를 그대로 담되, 중간에 깨져 있던 부분(코드 블록 잘림, "reinstall" 잘린 문구 등)은 unho님 SETUP.md 형식에 맞춰 다시 정리했고, 토큰을 대화창에 붙여넣지 말라는 보안 안내도 앞에 추가했습니다.

git으로 이 파일을 커밋·푸시할지는 아직 안 했습니다 — 필요하면 말씀해주세요.
