# 2026-07-04 unhoyeo

## 기획서 수작업 최종본 반영
- 자정 무렵, 직접 수작업으로 수정한 `기획서_TeamBrain_Port051 (3).docx`/`기획서.pdf`를 저장소에 올리고 push 완료.

## 구글 캘린더 자연어 일정 등록 + 알림 — 실사용 프로토타입 테스트
TeamBrain의 "자연어 일정 등록 + 팀 캘린더 + 리마인드 알림" 아이디어를, 본 프로젝트 코드에 앞서 구글 캘린더로 먼저 테스트해봄.

- 세팅: 개인 구글 계정으로 진행(팀 전체 캘린더 공유가 아니라 개인 계정 그대로 사용, "A안"). Google Cloud Console에 `Port-051` 프로젝트 생성 → Calendar API 활성화 → OAuth 클라이언트 발급 → 로컬 Python 가상환경에서 인증 진행.
- 팀 캘린더에 팀 일정(Port_051팀 07/11·07/18·07/25 12:00~14:00) 등록. 다른 팀(VAMOS, _LNK의전설) 일정은 넣지 않기로 함.
- **일정 이름 규칙 확정**: `멘토링 (멘토님 성함, 장소)`, `특강 (...)` 형식. (초반엔 `[장소] 멘토링 (성함)` 형식이었다가 팀 협의로 이 형식으로 변경됨 — 이후 팀 일정 등록 시 이 규칙 적용.)
- 팀원(김동연 `2020112071@dgu.ac.kr`, 신홍재 `shj2224@naver.com`) 이메일로 캘린더 공유(writer 권한, 일정 추가 가능).
- **일정 변경 시 팀원 전체에게 이메일 알림** 자동화: 처음엔 10분 간격 폴링 방식으로 구현했으나, "폴링 말고 등록 즉시 알림 가면 좋겠다"는 요구로 방향 전환 → Cloudflare Tunnel(`cloudflared`, ngrok 대신 계정가입 없이 사용) + Google Apps Script 기반 실시간 트리거 방식으로 재구현.
- 알림 메일 포맷 개선: 신규/변경/삭제를 명확히 구분하고, 날짜 형식을 "2026년 9월 5일 (토) 10:00"처럼 한국어 친화적으로 변경. 일정 삭제 시에도 알림이 가도록 로직 추가 및 실제 삭제 알림 발송 확인 완료.
- 팀원에게 설명할 3줄 요약("어떻게 자동 등록되게 했는지")도 별도로 준비함.

## iTerm 알림 연동
- Claude 응답이 끝나거나 사용자 입력이 필요할 때 iTerm으로 바로 전환되는 알림을 붙임 (`osascript` → 클릭 시 스크립트 편집기로 튀는 문제가 있어 `terminal-notifier`로 교체, 정상 작동 확인).
- Stop 훅과 수동 알림이 중복 발송되는 문제가 있었음 — 한때 훅을 지우고 수동으로만 보내려 했다가, 신뢰성 문제로 다시 훅 방식으로 복구. 훅 스크립트가 `jq` 경로(`/usr/bin/jq` vs `/opt/homebrew/bin/jq`) 문제로 세션 이름 대신 일반 메시지로 폴백되던 버그를 발견해 수정. 최종적으로 알림 제목=세션 이름(`/rename`으로 지정한 이름), 내용=실제 응답 텍스트로 개선 완료.

## 기타
- Claude Code 세션 이름 변경 방법 확인: `/rename <이름>` 슬래시 커맨드로 세션 내에서 바로 변경 가능(또는 시작 시 `claude -n <이름>`, 세션 목록에서 `Ctrl+R`).

## 결정된 것
- 팀 캘린더 일정은 개인 구글 계정 기반으로 운영(별도 팀 전용 계정 안 만듦).
- 멘토링/특강 일정 이름 형식: `멘토링 (성함, 장소)` / `특강 (...)`.
- 일정 등록/변경/삭제 시 팀원 전체에게 실시간(Apps Script 트리거 기반) 이메일 알림.

## 남은 질문 / 미해결
- 테스트 계정(`unhoyeo.dev@gmail.com`)으로 임시 전환했던 알림 수신자를 팀원 이메일로 되돌리는 작업, 테스트용 임시 일정 정리는 이 로그 내에서 완료된 것으로 보이나 최종 상태 재확인 필요.
- 이 구글 캘린더 프로토타입을 실제 TeamBrain 코드(Mattermost 어댑터)에 어떻게 이식할지는 아직 논의되지 않음.
