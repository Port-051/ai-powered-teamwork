## [turn] 2026-07-11 10:00:04

### user
아니다 그냥 기존 코드에서 특정 키워드 검색 이런 식으로 용도를 좁히면 오히려 ai한테 제약을 하는 거니까 mattermsot rest api에 있는 검색 조회 계열을 싹 다 mcp tool로 등록하고 사용법을 알려줘서 ai가 차라리 자율적으로 사용자의 질문에 대해서 무슨 도구를 써서 해야 할 지 하는 게 나을 거 같은데 어떻게 생각하지?

### assistant


동의는 하는데 범위를 하나만 조정하고 싶습니다. "검색/조회 계열을 여러 개 도구로 쪼개서 AI가 알아서 고르게 하자"는 방향 자체는 맞다고 봅니다 — 지금처럼 `search_mattermost` 하나에 다 우겨넣으면 "최근 대화 훑기"(`GetPostsForChannel`)나 "스레드 전체 보기"(`GetThread`) 같은, 키워드 검색과는 결이 다른 요청을 처리 못 하니까요.

다만 "Mattermost REST API 검색/조회 계열을 **싹 다**"는 조금 걱정됩니다. 아까 Hermes 공식 MCP 가이드에서도 "가장 작은 유용한 표면만 연결해라(smallest useful surface)", "위험한 도구 표면을 필터링할 준비가 안 됐으면 MCP 쓰지 마라"고 명시하고 있었죠. 지금 프로토타입엔 아직 비밀정보 필터링 레이어가 없는 상태라(README의 "아직 안 된 것"), 도구가 많아질수록 "필터링 안 된 채로 아무 채널이나 조회 가능한 통로"도 그만큼 늘어나는 셈입니다.

그래서 제 제안은: **읽기 전용(조회) 엔드포인트만 5~6개 정도로 나눠서** 등록하는 절충안입니다.
- `search_mattermost` (지금 것 — 키워드/from/in 검색)
- `get_channel_recent_posts` (키워드 없이 특정 채널 최근 메시지 시간순 조회)
- `get_thread` (post_id로 스레드 전체 보기 — 지난번에 얘기했던 그 빈자리)
- `get_user_by_username` / `get_user_by_id` (from: 쓸 때 username↔user_id 변환용)
- `list_channels_for_team` (봇이 접근 가능한 채널 목록 — "어느 채널에서 찾아야 할지" AI가 스스로 판단하게)

쓰기/관리 계열(채널 생성·삭제, 멤버 추방, 설정 변경 등)은 이번 프로토타입에서는 빼는 게 맞다고 봅니다. 이렇게 하면 "AI가 자율적으로 도구를 고른다"는 목적은 달성하면서, 표면은 여전히 "조회만" 이라는 선 안에 있습니다.

이 절충안으로 진행할까요, 아니면 정말 조회 계열 전부(채널 멤버 목록, 파일 검색 등 포함해서 더 많이)를 원하시나요?
