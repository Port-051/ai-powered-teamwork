## [turn] 2026-07-10 11:42:07

### user
2026-07-10 11:40:02,591 WARNING gateway.run: Unauthorized user: jem1fmmaopb6ipr57hp54o6hor (rlaehddus302) on mattermost
2026-07-10 11:40:09,470 WARNING gateway.channel_directory: Channel directory: failed to list Slack channels for team T0BFGPWUYQM: The request to the Slack API failed. (url: https://slack.com/api/users.conversations, status: 200)
The server responded with: {'ok': False, 'error': 'missing_scope', 'needed': 'channels:read,groups:read,mpim:read,im:read', 'provided': 'app_mentions:read,chat:write'} 설명

### assistant


두 개 다른 경고이고, 각각 원인과 해결법이 다릅니다.

**1. Mattermost — `Unauthorized user: jem1fmmaopb6ipr57hp54o6hor (rlaehddus302)`**

`rlaehddus302`님의 Mattermost User ID(`jem1fmmaopb6ipr57hp54o6hor`)가 그 profile의 `MATTERMOST_ALLOWED_USERS`에 안 들어있어서, 봇이 이 사람 메시지를 받고도 무시했다는 뜻입니다. 지난번에 정리한 문서에 나온 그대로 — "허용 안 된 사용자 메시지는 조용히 무시"되는데, 로그에는 WARNING으로 남는 겁니다.

해결:
```bash
# 이 profile의 .env
MATTERMOST_ALLOWED_USERS=<기존 ID들>,jem1fmmaopb6ipr57hp54o6hor
```
수정 후 게이트웨이 재시작 필요.

**2. Slack — `missing_scope: channels:read,groups:read,mpim:read,im:read`**

Slack 앱이 채널 목록을 가져오려고(`users.conversations` API) 했는데, 지금 봇에 부여된 권한(Scope)은 `app_mentions:read`, `chat:write` 두 개뿐이라 거부당한 겁니다. Hermes의 "channel directory"(채널 목록 캐싱) 기능이 채널을 나열하려면 아래 4개 scope가 추가로 필요합니다:
- `channels:read` (공개 채널 목록)
- `groups:read` (비공개 채널)
- `mpim:read` (그룹 DM)
- `im:read` (1:1 DM)

해결 — Slack 앱 관리 화면(api.slack.com/apps)에서:
1. **OAuth & Permissions → Scopes → Bot Token Scopes**로 이동
2. 위 4개 scope 추가
3. 페이지 위쪽에 "재설치 필요" 알림이 뜸 → **reinstall to workspace** 클릭 → Allow

봇이 지금 당장 멘션에 답하는 기능 자체는 (`app_mentions:read`, `chat:write`만으로도) 정상 동작합니다 — 이 경고는 "채널 목록 캐싱"이라는 부가 기능만 막혀 있는 상태라, 급하지 않으면 무시해도 되고 채널 자동완성/디렉토리 기능까지 쓰고 싶으면 위 scope를 추가하면 됩니다.
