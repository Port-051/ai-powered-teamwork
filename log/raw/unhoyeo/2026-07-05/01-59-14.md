## [turn] 2026-07-05 01:59:14

### user
아니 이렇게 하면 너가 새로 쓸때도 있고 안 쓸 때도 있다는거잖아. 이거 강제 안 돼?

### assistant






좋은 지적이에요 — "잊지 않기"에 의존하지 않고, 요약을 안 쓰면 애초에 응답이 안 끝나게 강제할 수 있어요. Stop 훅이 `decision: block`을 리턴하면 저를 다시 돌려보내서 요약을 쓰게 만들 수 있습니다. 이렇게 구성할게요.





Now update `iterm-notify.sh` to block the stop (forcing me to write the summary) whenever the summary file wasn't refreshed since the turn started:



Now register the `UserPromptSubmit` hook in global settings so every turn gets a fresh start-timestamp:

















Both cases work correctly — blocks when stale/missing, proceeds normally when fresh, and won't loop forever (`stop_hook_active` guard prevents a repeated block).



강제 장치를 추가했어요:

1. **새 훅** `mark-turn-start.sh`(`UserPromptSubmit`)가 매 턴 시작 시각을 기록.
2. `iterm-notify.sh`(Stop 훅)가 응답을 끝내려 할 때, 요약 파일이 이번 턴 시작 이후로 갱신 안 됐으면 `decision: block`을 리턴해서 **저를 강제로 다시 돌려보내** 요약을 쓰게 만듭니다. 무한루프 방지로 `stop_hook_active`(이미 한번 block된 재시도)일 땐 다시 막지 않고 통과시킴.

로컬에서 시뮬레이션 테스트로 block/통과 양쪽 다 정상 동작 확인했어요. 이제 제가 깜빡해도 구조적으로 막히니, 이번 응답 끝나고도 알림이 이 문장으로 뜨는지 한번 봐주세요.
