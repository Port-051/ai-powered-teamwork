## [turn] 2026-07-08 01:43:52

### user
1,2,를 어떻게 잘 만들까? 일단, 1,2를 잘 만들고 나서 3을 생각해보고싶은데

### assistant


1·2번을 잘 만드는 핵심 원칙부터 말씀드리면: **정교한 검색 기술(임베딩/RAG)보다 먼저, "얼마나 잘 추출하고, 얼마나 단순하게 유지하느냐"가 승부처**입니다.

**1) 요약 품질 — 어떻게**
- 그냥 "이 대화 요약해줘"가 아니라, **뽑아낼 항목을 스키마로 못박아서** LLM에게 시키는 게 핵심입니다. 예: `{ 결정사항: [...], 진행상황: [...], 열린질문: [...], 참고링크/사람: [...] }` 같은 고정된 JSON 형식으로 강제.
- 잡담까지 다 요약하면 노이즈만 쌓이니, **"이게 팀 지식으로 남길 가치가 있는 대화인가"를 먼저 거르는 1차 분류**를 거친 뒤에만 요약하는 게 좋습니다.
- 품질 확인은 감으로 하지 말고, **하루치 실제 대화를 사람이 직접 "이상적인 요약"으로 써보고, AI 결과랑 비교**하는 작은 평가셋을 만들어두면 나중에 프롬프트 바꿀 때마다 좋아졌는지 나빠졌는지 알 수 있습니다.

**2) 검색 품질 — 어떻게 (반전이 있습니다)**
- 소규모 팀 몇 개 대상 MVP 단계에서는, **사실 "검색"이라는 게 아예 필요 없을 수도 있습니다.** 왜냐면 요약축적형으로 몇 달치를 모아도 텍스트 양이 크지 않아서, **그냥 "팀 지식 문서 전체"를 매번 Claude 컨텍스트에 통째로 넣고 질문하면 끝**입니다 (지금 저와 여러분이 대화할 때 CLAUDE.md 전체를 매번 읽어들이는 것과 똑같은 방식).
- 이러면 임베딩·벡터DB·검색 알고리즘 자체가 필요 없어지고, 오직 "1번(요약을 잘 추출하는 것)"에만 집중하면 됩니다.
- 이 문서가 나중에 너무 커져서 컨텍스트에 안 들어갈 정도가 되면(수개월~수년 치 쌓이면) 그때 가서 진짜 검색(임베딩)을 얹으면 됩니다.

**정리**: 지금 단계에선 "검색 기술"은 아예 스킵하고, **1번(구조화된 요약 추출)만 잘 만드는 데 집중**하는 게 제일 효율적인 순서입니다. 이 방향으로, 실제로 하루치 대화 요약을 만드는 프롬프트/스크립트부터 짜볼까요?
