## [turn] 2026-07-07 14:41:38

### user
니가 말한 사이트에 토큰 발금받으면, XOXE밖에없는데?

### assistant


아, 그거면 **Bot Token Scopes를 안 넣고 설치하셔서 그런 것**일 가능성이 높아요. Slack은 "Bot Token Scopes"에 권한을 최소 1개 이상 추가해야 봇 토큰(`xoxb-`)을 만들어줍니다. 안 넣으면 사용자 토큰(`xoxe.xoxp-`)만 나와요.

확인해주세요:
1. **OAuth & Permissions** 페이지에서 아래로 스크롤
2. **"Scopes"** 섹션에 **"Bot Token Scopes"**라는 상자가 있나요? 거기 리스트가 비어있나요?

비어있으면:
1. 그 상자 밑 **"Add an OAuth Scope"** 버튼 클릭
2. `app_mentions:read`, `chat:write` 최소 이 두 개 추가
3. 페이지 맨 위로 올라가서 **"Reinstall to Workspace"** 클릭 → 권한 승인
4. 그러면 위쪽에 **"Bot User OAuth Token"**이 `xoxb-`로 새로 뜰 거예요

지금 Bot Token Scopes 상자에 뭐가 보이는지 확인해주시겠어요?
