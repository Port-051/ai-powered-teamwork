## [turn] 2026-07-12 12:21:38

### user
2로넘어가자. 근데 뭐 테스트하고끝이야?

### assistant








좋은 질문이에요. 이 레포 자체에는 **"고친 버전(fix)"이 따로 없습니다** — git 커밋 히스토리도 각 step 추가만 있고 브랜치/태그도 `main` 하나뿐이라, "장애를 재현하는 코드"까지만 들어있어요.

그래서 흐름은 보통 이렇게 됩니다:
1. **재현** — 지금처럼 부하를 걸어서 응답시간이 느려지는 걸 눈으로 확인 (여기까지가 방금 하신 것)
2. **진단** — `redis-cli`로 `SLOWLOG`, `MONITOR`, `INFO`, `LATENCY` 같은 명령으로 "무엇이 블로킹을 일으키는지" 직접 관찰
3. **수정** — 코드를 고쳐서 재현 (예: step_1은 `KEYS` → `SCAN`, step_2는 `LPOS` → 별도 인덱스/`ZRANK`, step_3는 `ZREVRANGE 0 -1` → `ZRANGEBYSCORE`+`LIMIT`)
4. 다시 부하를 걸어 **개선 확인**

이건 교육 과정이라 3번(수정)은 강사님이 강의에서 직접 다루실 수도 있어요. 원하시면 제가 진단(2번)까지 같이 해드리거나, 필요하면 수정 버전 코드도 같이 만들어볼 수 있습니다.

일단 step_1 프로세스 정리하고 step_2로 넘어갈게요.











지금 상황을 보니 확실히 자원을 먹고 있었어요 — 키가 벌써 **124만 개**, Redis 메모리 사용량 **140MB**, 그리고 `/points`가 계속 `KEYS`로 전체 스캔하느라 uvicorn 프로세스가 CPU를 **100%** 넘게 쓰고 있었습니다(장애 재현이 제대로 된 거긴 한데, 계속 두면 메모리가 무한정 늘어나요). 지금 종료할게요.
