## [turn] 2026-07-08 14:59:44

### user
지금 슬랙에 헤르메스 봇이 멘션하지 않아도 제멋대로 답장하는 중이야. 난 에이전트가 꼭 멘션을 해야 답장하도록 하고 싶어.

### assistant






config.yaml에 게이트웨이 관련 설정이 있을 것 같아 확인해볼게.







































확인해보니 원인은 설정 문제가 아니라 Hermes 코드 자체의 설계야. `~/.hermes/config.yaml`엔 이미 `require_mention: true` / `thread_require_mention: true`가 켜져 있는데, Slack 어댑터 코드(`plugins/platforms/slack/adapter.py:2826`)를 보면 **DM(1:1 대화)에서는 이 멘션 체크 로직 자체를 건너뛰도록** 짜여 있어(`if not is_dm and bot_uid:` — DM이면 이 블록을 아예 안 탐). 즉 "채널"에서는 멘션이 필수지만, "봇과의 DM/스레드"에서는 애초에 멘션이라는 개념이 없이(마치 사람한테 문자 보내듯) 뭘 쳐도 다 봇한테 보내는 걸로 취급하도록 하드코딩돼 있어. 이건 버그가 아니라 의도된 동작이고, config로 끌 수 있는 옵션이 없어.

두 가지 길이 있어:









좋은 소식은 이거 코드 패치 없이 이미 있는 설정으로 되는 거였어 — `strict_mention`이라는 옵션이 정확히 이 용도로 존재해("채널 스레드에서 매 메시지마다 명시적 멘션 필요, 스레드 기억/자동 응답 다 끔"). DM 로직에는 영향 없음(이미 확인함). config.yaml에 추가할게.





이제 launchd 서비스를 재시작해서 새 설정을 반영할게.
