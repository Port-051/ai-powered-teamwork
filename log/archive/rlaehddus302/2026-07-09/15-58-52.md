## [turn] 2026-07-09 15:58:52

### user
- 항상 가로로: 모션 설정과 무관하게 누구에게나 가로 슬라이드를 강제. 그리고 서버 내려

### assistant


가로 슬라이드를 모두에게 강제하도록 모션 최소화 폴백을 제거할게. JS와 CSS 두 군데를 고쳐야 해. 먼저 정확한 위치를 볼게.









Now the JS in FeatureDeck — remove the reduced-motion skip:





JS와 CSS 둘 다 수정했어. 이제 서버 재시작하고, **모션 최소화를 켠 상태(네 Windows 상황과 동일)** 로 에뮬레이트해서 가로가 되는지 검증할게. 먼저 타입체크:











PASS ✅ — 이번엔 **모션 최소화를 켠 상태(네 Windows 애니메이션 OFF와 동일)로 에뮬레이트**해서 검증했고, 가로 슬라이드가 정상 작동해 (tx 0 → -2880). 네 상황이 실제로 해결된 걸 확인했어.

이제 서버 내릴게.





완료. 서버 내려갔어 (내 종료 명령으로).

**정리한 것:**
- 모션 최소화 폴백을 제거해서, 이제 **Windows 애니메이션 설정과 무관하게 넓은 화면이면 누구에게나 가로 슬라이드가 강제**돼.
- 실제로 모션 최소화 켠 상태로 검증까지 마침 (PASS).
- 모바일(좁은 화면)에선 여전히 세로로 폴백 — 가로 조작이 안 되는 기기니까.

**변경된 파일** (아직 커밋 안 함):
- `landing/src/components/FeatureDeck.tsx` — 모션 최소화 시 가로 끄던 조건 제거
- `landing/src/styles.css` — 모션 최소화용 세로 폴백 블록 제거, smooth 스크롤 제거(휠 반응 개선)

다음에 확인할 땐 dev 서버 새로 띄우고, 브라우저는 **시크릿 창이나 강력 새로고침**으로 열면 돼 (`/mnt/c`라 캐시가 잘 남아서).

이 랜딩 페이지 변경들 커밋할까? (스냅 스크롤 + 가로 슬라이드 전체) 아니면 더 손볼 데 있으면 말해줘.
